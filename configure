#! /bin/sh










































# Guess values for system-dependent variables and create Makefiles.
# Generated automatically using autoconf version 2.10 
# Copyright (C) 1992, 93, 94, 95, 96 Free Software Foundation, Inc.
#
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.

# Defaults:
ac_help=
ac_default_prefix=/usr/local
# Any additions from configure.in:
  dummy="mmdfnodomlit=1"
  eval $dummy
  dummy="mmdfstatsort=1"
  eval $dummy
ac_default_prefix='/usr'
ac_help="$ac_help
  --with-mmdfprefix=MMDFPREFIX  (default is $mmdfprefix)"
ac_help="$ac_help
  --with-mmdflogin=MMDFLOGIN   (default is mmdf)"
ac_help="$ac_help
  --with-mmdfgroup=MMDFGROUP   (default is mmdf)"
ac_help="$ac_help
  --with-rootlogin=ROOTLOGIN   (default is root)"
ac_help="$ac_help
\
    --enable-PACKAGE        configure and build PACKAGE
     --disable-PACKAGE       do not configure and build PACKAGE
 where PACKAGE can be:
     nameserver           nameserver-code                          \
 (enabled)"
ac_help="$ac_help
\
       dbm                tables via dbm, sequential otherwise     \
 (enabled)"
ac_help="$ac_help
\
       wrap               tcpwrapper                               \
 (enabled)"
ac_help="$ac_help
\
       nis                nis support for tables                   \
(disabled)"
ac_help="$ac_help
\
       nn                 table mailids and users will be
                           configurable in mmdftailor               \
(disabled)"
ac_help="$ac_help
\
       nosrcroute         remove source-routing                    \
 (enabled)"
ac_help="$ac_help
\
       8bit               8bit clean                               \
 (enabled)"
ac_help="$ac_help
\
       lock_files         use explicily lock-files for filelocking \
(disabled)"
ac_help="$ac_help
\
       dial               dial code                                \
(disabled)"
ac_help="$ac_help
\
       badusers           badusers channel-program                 \
 (enabled)"
ac_help="$ac_help
\
       bboards            bboards channel-program                  \
(disabled)"
ac_help="$ac_help
\
       blockaddr          blockaddr channel-program                \
(disabled)"
ac_help="$ac_help
\
       delay              delay channel-program                    \
(disabled)"
ac_help="$ac_help
\
       ean                ean channel-program                      \
(disabled)"
ac_help="$ac_help
\
       list               list channel-program                     \
 (enabled)"
ac_help="$ac_help
\
       niftp              niftp channel-program                    \
(disabled)"
ac_help="$ac_help
\
       phone              phone channel-program                    \
(disabled)"
ac_help="$ac_help
\
       pobox              pobox channel-program                    \
(disabled)"
ac_help="$ac_help
\
       pop                pop channel-program                      \
 (enabled)"
ac_help="$ac_help
\
       prog               prog channel-program                     \
(disabled)"
ac_help="$ac_help
\
       smphone            smphone channel-program                  \
(disabled)"
ac_help="$ac_help
\
       smtp               smtp channel-program                     \
 (enabled)"
ac_help="$ac_help
\
       uucp               uucp channel-program                     \
 (enabled)"
ac_help="$ac_help
\
       msg                msg user agent                           \
 (enabled)"
ac_help="$ac_help
\
       other              malias,mlist,rcv*,resend,sendmail,v6mail \
 (enabled)"
ac_help="$ac_help
\
       send               send user agent                          \
 (enabled)"
ac_help="$ac_help
\
       snd                snd user agent                           \
 (enabled)"
ac_help="$ac_help
\
       ucbmail            ucbmail 'Mail' program                   \
 (enabled)"
ac_help="$ac_help
\
       unsupported        autores, cvmbox                          \
(disabled)"
ac_help="$ac_help
\
       gcc                use gcc as compiler                      \
 (enabled)"
ac_help="$ac_help
\
       fhs                set pathnames according to fhs-2.0 
                           (disable it for old pathnames)          \
 (enabled)"
ac_help="$ac_help
\
       elf                produce elf code, (only on sco)          \
 (enabled)"
ac_help="$ac_help
\
       esmtp              enable esmtp-'support' poorly implementation
                           better disable it                      \
 (disabled)"
ac_help="$ac_help
\
       esmtp_dsn          enable esmtp-dsn'support' (experimental)\
 (disabled)"
ac_help="$ac_help
\
       esmtp_8bitmime     enable esmtp-8bitmime'support' (experimental)\
 (disabled)"
ac_help="$ac_help
\
       wildcard           enable wildcard-'support' (experimental) \
 (disabled)"
ac_help="$ac_help
\
       rbl                enable rbl-support (Blacklist) (developpment) \
 (disabled)"
ac_help="$ac_help
\
       filelist           enable creation of a filelist           \
 (disabled)"

# Initialize some variables set by options.
# The variables have the same names as the options, with
# dashes changed to underlines.
build=NONE
cache_file=./config.cache
exec_prefix=NONE
host=NONE
no_create=
create_target=
nonopt=NONE
no_recursion=
prefix=NONE
program_prefix=NONE
program_suffix=NONE
program_transform_name=s,x,x,
silent=
site=
srcdir=
target=NONE
verbose=
x_includes=NONE
x_libraries=NONE
bindir='${exec_prefix}/bin'
sbindir='${exec_prefix}/sbin'
#libexecdir='${exec_prefix}/libexec'
libexecdir='${exec_prefix}/lib/mh'
datadir='${prefix}/share'
sysconfdir='${prefix}/etc'
sharedstatedir='${prefix}/com'
localstatedir='${prefix}/var'
libdir='${exec_prefix}/lib'
includedir='${prefix}/include'
oldincludedir='/usr/include'
infodir='${prefix}/info'
mandir='${prefix}/man'
varprefix='/var'
mmdfprefix='${prefix}/lib/mmdf'
mcmddir='${mmdfprefix}/lib'
mchndir='${mmdfprefix}/chans'
mtbldir='${mmdfprefix}/table'
# mquedir='${varprefix}/mmdf/home'
# mphsdir='${varprefix}/mmdf/log/phase'
# mlogdir='${varprefix}/mmdf/log'
mquedir='${varprefix}/spool/mmdf/home'
mphsdir='${varprefix}/state/mmdf'
mlogdir='${varprefix}/log/mmdf'
d_calllog='${mlogdir}/dial_log'
if test "$mmdfdebug"  = ""; then mmdfdebug=0;  fi
if test "$mmdfdlog"   = ""; then mmdfdlog=0;   fi
if test "$mmdfdbglog" = ""; then mmdfdbglog=0; fi
if test "$mmdfnodomlit" = ""; then mmdfnodomlit=0; fi
if test "$mmdfleftdots" = ""; then mmdfleftdots=0; fi
if test "$mmdfstatsort" = ""; then mmdfstatsort=0; fi
if test "$mmdfcitation" = ""; then mmdfcitation=0; fi
used_mcmddir=0
used_mchndir=0
used_mtbldir=0
used_mphsdir=0
used_mlogdir=0
used_d_calllog=0
used_mquedir=0

# Initialize some other variables.
subdirs=
MFLAGS= MAKEFLAGS=

ac_prev=
for ac_option
do

  # If the previous option needs an argument, assign it.
  if test -n "$ac_prev"; then
    eval "$ac_prev=\$ac_option"
    ac_prev=
    continue
  fi

  case "$ac_option" in
  -*=*) ac_optarg=`echo "$ac_option" | sed 's/[-_a-zA-Z0-9]*=//'` ;;
  *) ac_optarg= ;;
  esac

  # Accept the important Cygnus configure options, so we can diagnose typos.

  case "$ac_option" in

  -bindir | --bindir | --bindi | --bind | --bin | --bi)
    ac_prev=bindir ;;
  -bindir=* | --bindir=* | --bindi=* | --bind=* | --bin=* | --bi=*)
    bindir="$ac_optarg" ;;

  -build | --build | --buil | --bui | --bu)
    ac_prev=build ;;
  -build=* | --build=* | --buil=* | --bui=* | --bu=*)
    build="$ac_optarg" ;;

  -cache-file | --cache-file | --cache-fil | --cache-fi \
  | --cache-f | --cache- | --cache | --cach | --cac | --ca | --c)
    ac_prev=cache_file ;;
  -cache-file=* | --cache-file=* | --cache-fil=* | --cache-fi=* \
  | --cache-f=* | --cache-=* | --cache=* | --cach=* | --cac=* | --ca=* | --c=*)
    cache_file="$ac_optarg" ;;

  --create-target | --create-targe | --create-targ | --create-tar \
  | --create-ta | --create-t | --create | --creat )
    create_target=yes ;;

  -datadir | --datadir | --datadi | --datad | --data | --dat | --da)
    ac_prev=datadir ;;
  -datadir=* | --datadir=* | --datadi=* | --datad=* | --data=* | --dat=* \
  | --da=*)
    datadir="$ac_optarg" ;;

  -disable-* | --disable-*)
    ac_feature=`echo $ac_option|sed -e 's/-*disable-//'`
    # Reject names that are not valid shell variable names.
    if test -n "`echo $ac_feature| sed 's/[-a-zA-Z0-9_]//g'`"; then
      { echo "configure: error: $ac_feature: invalid feature name" 1>&2; exit 1; }
    fi
    ac_feature=`echo $ac_feature| sed 's/-/_/g'`
    eval "enable_${ac_feature}=no" ;;

  -enable-* | --enable-*)
    ac_feature=`echo $ac_option|sed -e 's/-*enable-//' -e 's/=.*//'`
    # Reject names that are not valid shell variable names.
    if test -n "`echo $ac_feature| sed 's/[-_a-zA-Z0-9]//g'`"; then
      { echo "configure: error: $ac_feature: invalid feature name" 1>&2; exit 1; }
    fi
    ac_feature=`echo $ac_feature| sed 's/-/_/g'`
    case "$ac_option" in
      *=*) ;;
      *) ac_optarg=yes ;;
    esac
    eval "enable_${ac_feature}='$ac_optarg'" ;;

  -exec-prefix | --exec_prefix | --exec-prefix | --exec-prefi \
  | --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \
  | --exec | --exe | --ex)
    ac_prev=exec_prefix ;;
  -exec-prefix=* | --exec_prefix=* | --exec-prefix=* | --exec-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* | --ex=*)
    exec_prefix="$ac_optarg" ;;

  -gas | --gas | --ga | --g)
    # Obsolete; use --with-gas.
    with_gas=yes ;;

  -help | --help | --hel | --he)
    # Omit some internal or obsolete options to make the list less imposing.
    # This message is too long to be a string in the A/UX 3.1 sh.
    cat << EOF
Usage: configure [options] [host]
Options: [defaults in brackets after descriptions]
Configuration:
  --cache-file=FILE       cache test results in FILE
  --help                  print this message
  --no-create             do not create output files
  --quiet, --silent       do not print \`checking...' messages
  --version               print the version of autoconf that created configure

  --create-target         force creation of conf/<target>/* if needed
  --mmdf-debug=[0,1,2]    enable more logging information      [default: $mmdfdebug]
  --mmdf-dlog             enable debug-option of dial packages [default: $mmdfdlog]
  --mmdf-dbglog           enable more debug-option of dial packages [default: $mmdfdbglog]
  --mmdf-nodomlit         toggle define NODOMLIT [default: $mmdfnodomlit]
  --mmdf-leftdots         toggle define LEFTDOTS [default: $mmdfleftdots]
  --mmdf-statsort         toggle define STATSORT [default: $mmdfstatsort]
  --mmdf-citation         toggle define CITATION [default: $mmdfcitation]

Directory and file names:
  --prefix=PREFIX         install architecture-independent files in PREFIX
                          [$ac_default_prefix]
  --exec-prefix=EPREFIX   install architecture-dependent files in EPREFIX
                          [same as prefix]
  --mmdf-prefix=DIR       default home of mmdf in DIR [PREFIX/mmdf]
  --bindir=DIR            user executables in DIR [EPREFIX/bin]
  --sbindir=DIR           system admin executables in DIR [EPREFIX/sbin]
  --libexecdir=DIR        program executables in DIR [EPREFIX/libexec]
  --datadir=DIR           read-only architecture-independent data in DIR
                          [PREFIX/share]
  --sysconfdir=DIR        read-only single-machine data in DIR [PREFIX/etc]
  --sharedstatedir=DIR    modifiable architecture-independent data in DIR
                          [PREFIX/com]
  --localstatedir=DIR     modifiable single-machine data in DIR [PREFIX/var]
  --libdir=DIR            object code libraries in DIR [EPREFIX/lib]
  --includedir=DIR        C header files in DIR [PREFIX/include]
  --oldincludedir=DIR     C header files for non-gcc in DIR [/usr/include]
  --infodir=DIR           info documentation in DIR [PREFIX/info]
  --mandir=DIR            man documentation in DIR [PREFIX/man]
  --srcdir=DIR            find the sources in DIR [configure dir or ..]
  --varprefix=DIR         x in DIR [PREFIX/var]

  --mlogdir=DIR           x in DIR [VARPREFIX/mmdf/log]
  --mphsdir=DIR           x in DIR [VARPREFIX/mmdf/log/phase]
  --mcmddir=DIR           x in DIR [MMDFPREFIX/lib]
  --mchndir=DIR           x in DIR [MMDFPREFIX/chans]
  --mtbldir=DIR           x in DIR [MMDFPREFIX/table]
  --d_calllog=FILE        x in FILE [MLOGDIR/dial_log]

  --mmdf-spooldir=DIR     obselete --mquedir
  --mmdf-logdir=DIR       obselete --mlogdir
  --mmdf-phasedir=DIR     obselete --mphsdir
  --mmdf-diallog=FILE     obselete --d_calllog

  --program-prefix=PREFIX prepend PREFIX to installed program names
  --program-suffix=SUFFIX append SUFFIX to installed program names
  --program-transform-name=PROGRAM
                          run sed PROGRAM on installed program names
EOF
    cat << EOF
Host type:
  --build=BUILD           configure for building on BUILD [BUILD=HOST]
  --host=HOST             configure for HOST [guessed]
  --target=TARGET         configure for TARGET [TARGET=HOST]
Features and packages:
  --disable-FEATURE       do not include FEATURE (same as --enable-FEATURE=no)
  --enable-FEATURE[=ARG]  include FEATURE [ARG=yes]
  --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --x-includes=DIR        X include files are in DIR
  --x-libraries=DIR       X library files are in DIR
EOF
    if test -n "$ac_help"; then
      echo "--enable and --with options recognized:$ac_help"
    fi
    exit 0 ;;

  -host | --host | --hos | --ho)
    ac_prev=host ;;
  -host=* | --host=* | --hos=* | --ho=*)
    host="$ac_optarg" ;;

  -includedir | --includedir | --includedi | --included | --include \
  | --includ | --inclu | --incl | --inc)
    ac_prev=includedir ;;
  -includedir=* | --includedir=* | --includedi=* | --included=* | --include=* \
  | --includ=* | --inclu=* | --incl=* | --inc=*)
    includedir="$ac_optarg" ;;

  -infodir | --infodir | --infodi | --infod | --info | --inf)
    ac_prev=infodir ;;
  -infodir=* | --infodir=* | --infodi=* | --infod=* | --info=* | --inf=*)
    infodir="$ac_optarg" ;;

  -libdir | --libdir | --libdi | --libd)
    ac_prev=libdir ;;
  -libdir=* | --libdir=* | --libdi=* | --libd=*)
    libdir="$ac_optarg" ;;

  -libexecdir | --libexecdir | --libexecdi | --libexecd | --libexec \
  | --libexe | --libex | --libe)
    ac_prev=libexecdir ;;
  -libexecdir=* | --libexecdir=* | --libexecdi=* | --libexecd=* | --libexec=* \
  | --libexe=* | --libex=* | --libe=*)
    libexecdir="$ac_optarg" ;;

  -localstatedir | --localstatedir | --localstatedi | --localstated \
  | --localstate | --localstat | --localsta | --localst \
  | --locals | --local | --loca | --loc | --lo)
    ac_prev=localstatedir ;;
  -localstatedir=* | --localstatedir=* | --localstatedi=* | --localstated=* \
  | --localstate=* | --localstat=* | --localsta=* | --localst=* \
  | --locals=* | --local=* | --loca=* | --loc=* | --lo=*)
    localstatedir="$ac_optarg" ;;

  -mandir | --mandir | --mandi | --mand | --man | --ma | --m)
    ac_prev=mandir ;;
  -mandir=* | --mandir=* | --mandi=* | --mand=* | --man=* | --ma=* | --m=*)
    mandir="$ac_optarg" ;;

  -mlogdir | --mlogdir | --mlogdi | --mlogd | --mlog)
    ac_prev=mlogdir ;;
  -mlogdir=* | --mlogdir=* | --mlogdi=* | --mlogd=* | --mlog=*)
    mlogdir="$ac_optarg"; used_mlogdir=1 ;;

  -mmdf-citation | --mmdf-citation | --mmdf-citatio | --mmdf-citati \
  | --mmdf-citat | --mmdf-cita | --mmdf-cit | --mmdf-ci | --mmdf-c )
    ac_prev=mmdfcitation ;;
  -mmdf-citation=* | --mmdf-citation=* | --mmdf-citatio=* | --mmdf-citati=* \
  | --mmdf-citat=* | --mmdf-cita=* | --mmdf-cit=* | --mmdf-ci=* \
  | --mmdf-c=* )
    mmdfcitation="$ac_optarg" ;;

  -mmdf-dbglog | --mmdf-dbglog | --mmdf-dbglo | --mmdf-dbgl \
  | --mmdf-dbg | --mmdf-db )
    if test "$mmdfdbglog" = 0; then
	mmdfdbglog=1 ;
    else
	mmdfdbglog=0 ;
    fi;;

  -mmdf-debug | --mmdf-debug | --mmdf-debu | --mmdf-deb | --mmdf-de )
    ac_prev=mmdfdebug ;;
  -mmdf-debug=* | --mmdf-debug=* | --mmdf-debu=* | --mmdf-deb=* | --mmdf-de=* )
    mmdfdebug="$ac_optarg" ;;

  -d_calllog | --d_calllog | --d_calllo | --d_calll | --d_call)
    ac_prev=d_calllog ;;
  -d_calllog=* | --d_calllog=* | --d_calllo=* | --d_calll=* | --d_call=*)
    d_calllog="$ac_optarg"; used_d_calllog=1 ;;

  -mmdf-dlog | --mmdf-dlog | --mmdf-dlo | --mmdf-dl )
    if test "$mmdfdlog" = 0; then
        mmdfdlog=1 ;
    else
	mmdfdlog=0 ;
    fi;;

  -mmdf-leftdots | --mmdf-leftdots | --mmdf-leftdot | --mmdf-leftdo \
  | --mmdf-leftd | --mmdf-left | --mmdf-lef | --mmdf-le )
    if test "$mmdfleftdots" = 0; then
	mmdfleftdots=1
    else
	mmdfleftdots=0
    fi ;;

  -mmdf-nodomlit | --mmdf-nodomlit | --mmdf-nodomli | --mmdf-nodoml \
  | --mmdf-nodom | --mmdf-nodo | --mmdf-nod | --mmdf-no | --mmdf-n )
    if test "$mmdfnodomlit" = 0; then
	mmdfnodomlit=1
    else
	mmdfnodomlit=0
    fi ;;

  -mmdf-prefix | --mmdf-prefix | --mmdf-prefi | --mmdf-pref | --mmdf-pre \
  | --mmdf-pr )
    ac_prev=mmdfprefix ;;
  -mmdf-prefix=* | --mmdf-prefix=* | --mmdf-prefi=* | --mmdf-pref=* \
  | --mmdf-pre=* | --mmdf-pr=* )
    mmdfprefix="$ac_optarg"; used_mmdfprefix=1 ;;

  -mmdf-statsort | --mmdf-statsort | --mmdf-statsor | --mmdf-statso \
  | --mmdf-stats | --mmdf-stat | --mmdf-sta | --mmdf-st )
    if test "$mmdfstatsort" = "0"; then
	mmdfstatsort=1
    else
	mmdfstatsort=0
    fi ;;

  -mcmddir | --mcmddir | --mcmddi | --mcmdd | --mcmd)
    ac_prev=mcmddir ;;
  -mcmddir=* | --mcmddir=* | --mcmddi=* | --mcmdd=* | --mcmd=*)
    mcmddir="$ac_optarg"; used_mcmddir=1 ;;

  -mchndir | --mchndir | --mchndi | --mchnd | --mchn)
    ac_prev=mchndir ;;
  -mchndir=* | --mchndir=* | --mchndi=* | --mchnd=* | --mchn=*)
    mchndir="$ac_optarg"; used_mchndir=1 ;;

  -mtbldir | --mtbldir | --mtbldi | --mtbld | --mtbl)
    ac_prev=mtbldir ;;
  -mtbldir=* | --mtbldir=* | --mtbldi=* | --mtbld=* | --mtbl=*)
    mtbldir="$ac_optarg"; used_mtbldir=1 ;;

  -mphsdir | --mphsdir | --mphsdi | --mphsd | --mphs)
    ac_prev=mphsdir ;;
  -mphsdir=* | --mphsdir=* | --mphsdi=* | --mphsd=* | --mphs=*)
    mphsdir="$ac_optarg"; used_mphsdir=1 ;;

  -mquedir | --mquedir | --mquedi | --mqued | --mque)
    ac_prev=mquedir ;;
  -mquedir=* | --mquedir=* | --mquedi=* | --mqued=* | --mque=*)
    mquedir="$ac_optarg"; used_mquedir=1 ;;

  -nfp | --nfp | --nf)
    # Obsolete; use --without-fp.
    with_fp=no ;;

  -no-create | --no-create | --no-creat | --no-crea | --no-cre \
  | --no-cr | --no-c)
    no_create=yes ;;

  -no-recursion | --no-recursion | --no-recursio | --no-recursi \
  | --no-recurs | --no-recur | --no-recu | --no-rec | --no-re | --no-r)
    no_recursion=yes ;;

  -oldincludedir | --oldincludedir | --oldincludedi | --oldincluded \
  | --oldinclude | --oldinclud | --oldinclu | --oldincl | --oldinc \
  | --oldin | --oldi | --old | --ol | --o)
    ac_prev=oldincludedir ;;
  -oldincludedir=* | --oldincludedir=* | --oldincludedi=* | --oldincluded=* \
  | --oldinclude=* | --oldinclud=* | --oldinclu=* | --oldincl=* | --oldinc=* \
  | --oldin=* | --oldi=* | --old=* | --ol=* | --o=*)
    oldincludedir="$ac_optarg" ;;

  -prefix | --prefix | --prefi | --pref | --pre | --pr | --p)
    ac_prev=prefix ;;
  -prefix=* | --prefix=* | --prefi=* | --pref=* | --pre=* | --pr=* | --p=*)
    prefix="$ac_optarg" ;;

  -program-prefix | --program-prefix | --program-prefi | --program-pref \
  | --program-pre | --program-pr | --program-p)
    ac_prev=program_prefix ;;
  -program-prefix=* | --program-prefix=* | --program-prefi=* \
  | --program-pref=* | --program-pre=* | --program-pr=* | --program-p=*)
    program_prefix="$ac_optarg" ;;

  -program-suffix | --program-suffix | --program-suffi | --program-suff \
  | --program-suf | --program-su | --program-s)
    ac_prev=program_suffix ;;
  -program-suffix=* | --program-suffix=* | --program-suffi=* \
  | --program-suff=* | --program-suf=* | --program-su=* | --program-s=*)
    program_suffix="$ac_optarg" ;;

  -program-transform-name | --program-transform-name \
  | --program-transform-nam | --program-transform-na \
  | --program-transform-n | --program-transform- \
  | --program-transform | --program-transfor \
  | --program-transfo | --program-transf \
  | --program-trans | --program-tran \
  | --progr-tra | --program-tr | --program-t)
    ac_prev=program_transform_name ;;
  -program-transform-name=* | --program-transform-name=* \
  | --program-transform-nam=* | --program-transform-na=* \
  | --program-transform-n=* | --program-transform-=* \
  | --program-transform=* | --program-transfor=* \
  | --program-transfo=* | --program-transf=* \
  | --program-trans=* | --program-tran=* \
  | --progr-tra=* | --program-tr=* | --program-t=*)
    program_transform_name="$ac_optarg" ;;

  -q | -quiet | --quiet | --quie | --qui | --qu | --q \
  | -silent | --silent | --silen | --sile | --sil)
    silent=yes ;;

  -sbindir | --sbindir | --sbindi | --sbind | --sbin | --sbi | --sb)
    ac_prev=sbindir ;;
  -sbindir=* | --sbindir=* | --sbindi=* | --sbind=* | --sbin=* \
  | --sbi=* | --sb=*)
    sbindir="$ac_optarg" ;;

  -sharedstatedir | --sharedstatedir | --sharedstatedi \
  | --sharedstated | --sharedstate | --sharedstat | --sharedsta \
  | --sharedst | --shareds | --shared | --share | --shar \
  | --sha | --sh)
    ac_prev=sharedstatedir ;;
  -sharedstatedir=* | --sharedstatedir=* | --sharedstatedi=* \
  | --sharedstated=* | --sharedstate=* | --sharedstat=* | --sharedsta=* \
  | --sharedst=* | --shareds=* | --shared=* | --share=* | --shar=* \
  | --sha=* | --sh=*)
    sharedstatedir="$ac_optarg" ;;

  -site | --site | --sit)
    ac_prev=site ;;
  -site=* | --site=* | --sit=*)
    site="$ac_optarg" ;;

  -srcdir | --srcdir | --srcdi | --srcd | --src | --sr)
    ac_prev=srcdir ;;
  -srcdir=* | --srcdir=* | --srcdi=* | --srcd=* | --src=* | --sr=*)
    srcdir="$ac_optarg" ;;

  -sysconfdir | --sysconfdir | --sysconfdi | --sysconfd | --sysconf \
  | --syscon | --sysco | --sysc | --sys | --sy)
    ac_prev=sysconfdir ;;
  -sysconfdir=* | --sysconfdir=* | --sysconfdi=* | --sysconfd=* | --sysconf=* \
  | --syscon=* | --sysco=* | --sysc=* | --sys=* | --sy=*)
    sysconfdir="$ac_optarg" ;;

  -target | --target | --targe | --targ | --tar | --ta | --t)
    ac_prev=target ;;
  -target=* | --target=* | --targe=* | --targ=* | --tar=* | --ta=* | --t=*)
    target="$ac_optarg" ;;

  -varprefix | --varprefix | --varprefi | --varpref | --varpre | --varpr \
  | --varp | --var)
    ac_prev=varprefix ;;
  --varprefix=* | --varprefix=* | --varprefi=* | --varpref=* | --varpre=* \
  | --varpr=* | --varp=* | --var=*)
    varprefix="$ac_optarg" ;;

  -v | -verbose | --verbose | --verbos | --verbo | --verb)
    verbose=yes ;;

  -version | --version | --versio | --versi | --vers)
    echo "configure generated by autoconf version 2.10"
    exit 0 ;;

  -with-* | --with-*)
    ac_package=`echo $ac_option|sed -e 's/-*with-//' -e 's/=.*//'`
    # Reject names that are not valid shell variable names.
    if test -n "`echo $ac_package| sed 's/[-_a-zA-Z0-9]//g'`"; then
      { echo "configure: error: $ac_package: invalid package name" 1>&2; exit 1; }
    fi
    ac_package=`echo $ac_package| sed 's/-/_/g'`
    case "$ac_option" in
      *=*) ;;
      *) ac_optarg=yes ;;
    esac
    eval "with_${ac_package}='$ac_optarg'" ;;

  -without-* | --without-*)
    ac_package=`echo $ac_option|sed -e 's/-*without-//'`
    # Reject names that are not valid shell variable names.
    if test -n "`echo $ac_package| sed 's/[-a-zA-Z0-9_]//g'`"; then
      { echo "configure: error: $ac_package: invalid package name" 1>&2; exit 1; }
    fi
    ac_package=`echo $ac_package| sed 's/-/_/g'`
    eval "with_${ac_package}=no" ;;

  --x)
    # Obsolete; use --with-x.
    with_x=yes ;;

  -x-includes | --x-includes | --x-include | --x-includ | --x-inclu \
  | --x-incl | --x-inc | --x-in | --x-i)
    ac_prev=x_includes ;;
  -x-includes=* | --x-includes=* | --x-include=* | --x-includ=* | --x-inclu=* \
  | --x-incl=* | --x-inc=* | --x-in=* | --x-i=*)
    x_includes="$ac_optarg" ;;

  -x-libraries | --x-libraries | --x-librarie | --x-librari \
  | --x-librar | --x-libra | --x-libr | --x-lib | --x-li | --x-l)
    ac_prev=x_libraries ;;
  -x-libraries=* | --x-libraries=* | --x-librarie=* | --x-librari=* \
  | --x-librar=* | --x-libra=* | --x-libr=* | --x-lib=* | --x-li=* | --x-l=*)
    x_libraries="$ac_optarg" ;;

  -mmdf-logdir | --mmdf-logdir | --mmdf-logdi | --mmdf-logd | --mmdf-log \
  | --mmdf-lo )
    ac_prev=mlogdir ;;
  -mmdf-logdir=* | --mmdf-logdir=* | --mmdf-logdi=* | --mmdf-logd=* | --mmdf-log=* \
  | --mmdf-lo=* )
    mlogdir="$ac_optarg"; used_mlogdir=1 ;;

  -mmdf-phasedir | --mmdf-phasedir | --mmdf-phasedi | --mmdf-phased | --mmdf-phase | --mmdf-phas \
  | --mmdf-pha | --mmdf-ph)
    ac_prev=mphsdir ;;
  -mmdf-phasedir=* | --mmdf-phasedir=* | --mmdf-phasedi=* | --mmdf-phased=* | --mmdf-phase=* \
  | --mmdf-phas=* | --mmdf-pha=* | --mmdf-ph=*)
    mphsdir="$ac_optarg"; used_mphsdir=1 ;;

  -mmdf-spooldir | --mmdf-spooldir | --mmdf-spooldi | --mmdf-spoold | --mmdf-spool | --mmdf-spoo \
  | --mmdf-spo | --mmdf-sp)
    ac_prev=mquedir ;;
  -mmdf-spooldir=* | --mmdf-spooldir=* | --mmdf-spooldi=* | --mmdf-spoold=* | --mmdf-spool=* \
  | --mmdf-spoo=* | --mmdf-spo=* | --mmdf-sp=*)
    mquedir="$ac_optarg"; used_mquedir=1 ;;
  -mmdf-diallog | --mmdf-diallog | --mmdf-diallo | --mmdf-diall | --mmdf-dial | --mmdf-dia)
    ac_prev=mmdfdiallog ;;
  -mmdf-diallog=* | --mmdf-diallog=* | --mmdf-diallo=* | --mmdf-diall=* | --mmdf-dial=* | --mmdf-dia=*)
    mmdfdiallog="$ac_optarg"; used_mmdfdiallog=1 ;;

  -*) { echo "configure: error: $ac_option: invalid option; use --help to show usage" 1>&2; exit 1; }
    ;;

  *)
    if test -n "`echo $ac_option| sed 's/[-a-z0-9.]//g'`"; then
      echo "configure: warning: $ac_option: invalid host type" 1>&2
    fi
    if test "x$nonopt" != xNONE; then
      { echo "configure: error: can only configure for one host and one target at a time" 1>&2; exit 1; }
    fi
    nonopt="$ac_option"
    ;;

  esac
done

if test -n "$ac_prev"; then
  { echo "configure: error: missing argument to --`echo $ac_prev | sed 's/_/-/g'`" 1>&2; exit 1; }
fi

trap 'rm -fr conftest* confdefs* core core.* *.core $ac_clean_files; exit 1' 1 2 15

# File descriptor usage:
# 0 standard input
# 1 file creation
# 2 errors and warnings
# 3 some systems may open it to /dev/tty
# 4 used on the Kubota Titan
# 6 checking for... messages and results
# 5 compiler messages saved in config.log
if test "$silent" = yes; then
  exec 6>/dev/null
else
  exec 6>&1
fi
exec 5>./config.log

echo "\
This file contains any messages produced by compilers while
running configure, to aid debugging if configure makes a mistake.
" 1>&5

# Strip out --no-create and --no-recursion so they do not pile up.
# Also quote any args containing shell metacharacters.
ac_configure_args=
for ac_arg
do
  case "$ac_arg" in
  -no-create | --no-create | --no-creat | --no-crea | --no-cre \
  | --no-cr | --no-c) ;;
  -no-recursion | --no-recursion | --no-recursio | --no-recursi \
  | --no-recurs | --no-recur | --no-recu | --no-rec | --no-re | --no-r) ;;
  *" "*|*"	"*|*[\[\]\~\#\$\^\&\*\(\)\{\}\\\|\;\<\>\?]*)
  ac_configure_args="$ac_configure_args '$ac_arg'" ;;
  *) ac_configure_args="$ac_configure_args $ac_arg" ;;
  esac
done

# NLS nuisances.
# Only set LANG and LC_ALL to C if already set.
# These must not be set unconditionally because not all systems understand
# e.g. LANG=C (notably SCO).
if test "${LC_ALL+set}" = set; then LC_ALL=C; export LC_ALL; fi
if test "${LANG+set}"   = set; then LANG=C;   export LANG;   fi

# confdefs.h avoids OS command line length limits that DEFS can exceed.
rm -rf conftest* confdefs.h
# AIX cpp loses on an empty file, so make sure it contains at least a newline.
echo > confdefs.h

# A filename unique to this package, relative to the directory that
# configure is in, which we can look for to find out if srcdir is correct.
ac_unique_file=h/ap.h

# Find the source files, if location was not specified.
if test -z "$srcdir"; then
  ac_srcdir_defaulted=yes
  # Try the directory containing this script, then its parent.
  ac_prog=$0
  ac_confdir=`echo $ac_prog|sed 's%/[^/][^/]*$%%'`
  test "x$ac_confdir" = "x$ac_prog" && ac_confdir=.
  srcdir=$ac_confdir
  if test ! -r $srcdir/$ac_unique_file; then
    srcdir=..
  fi
else
  ac_srcdir_defaulted=no
fi
if test ! -r $srcdir/$ac_unique_file; then
  if test "$ac_srcdir_defaulted" = yes; then
    { echo "configure: error: can not find sources in $ac_confdir or .." 1>&2; exit 1; }
  else
    { echo "configure: error: can not find sources in $srcdir" 1>&2; exit 1; }
  fi
fi
srcdir=`echo "${srcdir}" | sed 's%\([^/]\)/*$%\1%'`

# Prefer explicitly selected file to automatically selected ones.
if test -z "$CONFIG_SITE"; then
  if test "x$prefix" != xNONE; then
    CONFIG_SITE="$prefix/share/config.site $prefix/etc/config.site"
  else
    CONFIG_SITE="$ac_default_prefix/share/config.site $ac_default_prefix/etc/config.site"
  fi
fi
for ac_site_file in $CONFIG_SITE; do
  if test -r "$ac_site_file"; then
    echo "loading site script $ac_site_file"
    . "$ac_site_file"
  fi
done

if test -r "$cache_file"; then
  echo "loading cache $cache_file"
  . $cache_file
else
  echo "creating cache $cache_file"
  > $cache_file
fi

ac_ext=c
# CFLAGS is not in ac_cpp because -g, -O, etc. are not valid cpp options.
ac_cpp='$CPP $CPPFLAGS'
ac_compile='${CC-cc} -c $CFLAGS $CPPFLAGS conftest.$ac_ext 1>&5'
ac_link='${CC-cc} -o conftest $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS 1>&5'

if (echo "testing\c"; echo 1,2,3) | grep c >/dev/null; then
  # Stardent Vistra SVR4 grep lacks -e, says ghazi@caip.rutgers.edu.
  if (echo -n testing; echo 1,2,3) | sed s/-n/xn/ | grep xn >/dev/null; then
    ac_n= ac_c='
' ac_t='	'
  else
    ac_n=-n ac_c= ac_t=
  fi
else
  ac_n= ac_c='\c' ac_t=
fi









OUTPUT_FILES="lib/Makefile src/Makefile uip/Makefile"
OUTPUT_FILES=$OUTPUT_FILES" conf/common/Makefile.com Makefile"
OUTPUT_FILES=$OUTPUT_FILES" man/Makefile"

VERSION=`sed -e 's/MMDF //' -e 's/\./ /' ${srcdir}/Version`
MMDF_MAJOR_VERSION=`echo $VERSION | awk '{print $1}'`
MMDF_MINOR_VERSION=`echo $VERSION | awk '{print $2}'`
MMDF_PATCH_LEVEL=`echo $VERSION | awk '{print $3}'`
ARG4=`echo $VERSION | awk '{print $4}'`
ARG5=`echo $VERSION | awk '{print $5}'`
if test "$ARG5" = ""; then
  VERSION_DATE="$MMDF_PATCH_LEVEL $ARG4"
  MMDF_PATCH_LEVEL=""
  echo "configuring for MMDF-$MMDF_MAJOR_VERSION.$MMDF_MINOR_VERSION \
 ($VERSION_DATE)"
else
  VERSION_DATE="$ARG4 $ARG5"
  echo "configuring for MMDF-$MMDF_MAJOR_VERSION.$MMDF_MINOR_VERSION \
  Patchlevel $MMDF_PATCH_LEVEL ($VERSION_DATE)"
fi
VERSION="MMDF-$MMDF_MAJOR_VERSION.$MMDF_MINOR_VERSION"

if test "${with_mmdfprefix+set}" = set; then
  mmdfprefix=${with_mmdfprefix}
fi
if test "${with_mmdflogin+set}" != set; then
  cat >> confdefs.h <<EOF
#define MMDFLOGIN "mmdf"
EOF

  mmdflogin=mmdf
else
  cat >> confdefs.h <<EOF
#define MMDFLOGIN "${with_mmdflogin}"
EOF

  mmdflogin=${with_mmdflogin}
fi

if test "${with_mmdfgroup+set}" != set; then
  cat >> confdefs.h <<EOF
#define MMDFGROUP "mmdf"
EOF

  mmdfgroup=mmdf
else
  cat >> confdefs.h <<EOF
#define MMDFGROUP "${with_mmdfgroup}"
EOF

  mmdfgroup=${with_mmdfgroup}
fi

if test "${with_rootlogin+set}" != set; then
  ROOTLOGIN=root
else
  ROOTLOGIN=${with_rootlogin}
fi

PGMPROT=511
MMDFLIBS="\${top_srcdir}/lib/libmmdf.a"
MMDF_TARGET=no
rcvdir="${libexecdir}"


  UNAME_MACHINE=`(uname -m) 2>/dev/null` || UNAME_MACHINE=unknown
  UNAME_NODE=`(uname -n) 2>/dev/null` || UNAME_NODE=unknown
  UNAME_RELEASE=`(uname -r) 2>/dev/null` || UNAME_RELEASE=unknown
  UNAME_SYSTEM=`(uname -s) 2>/dev/null` || UNAME_SYSTEM=unknown
  UNAME_VERSION=`(uname -v) 2>/dev/null` || UNAME_VERSION=unknown

MMDF_CONFIG=$UNAME_NODE.config

  echo $ac_n "checking for local config '$MMDF_CONFIG'""... $ac_c" 1>&6
  if test ! -x "conf"; then
     mkdir conf
  fi
  if test ! -x "conf/sites"; then
     mkdir conf/sites
  fi
  if test -r conf/sites/$MMDF_CONFIG; then
    echo "$ac_t""yes" 1>&6
    echo "loading local configuration '"$MMDF_CONFIG"'"
    . conf/sites/$MMDF_CONFIG
    for i in `set | grep CONFIG_SYS_`
    do
      var=`echo $i | sed s/CONFIG_SYS_//`
      eval D_$var
    done
    for i in `set | grep CONFIG_CODE_`
    do
      var=`echo $i | tr 'ABCDEFGHIJKLMNOPQRSTUVWXYZ' 'abcdefghijklmnopqrstuvwxyz'| sed s/config_/enable_/`
      eval ac_cv_$var
    done
    for i in `set | grep CONFIG_LIB_`
    do
      var=`echo $i | tr 'ABCDEFGHIJKLMNOPQRSTUVWXYZ' 'abcdefghijklmnopqrstuvwxyz'| sed s/config_lib_/enable_/`
      eval ac_cv_$var
    done
    for i in `set | grep CONFIG_SRC_`
    do
      var=`echo $i | tr 'ABCDEFGHIJKLMNOPQRSTUVWXYZ' 'abcdefghijklmnopqrstuvwxyz'| sed s/config_src_/enable_/`
      eval ac_cv_$var
    done
    for i in `set | grep CONFIG_UIP_`
    do
      var=`echo $i | tr 'ABCDEFGHIJKLMNOPQRSTUVWXYZ' 'abcdefghijklmnopqrstuvwxyz'| sed s/config_uip_/enable_/`
      eval ac_cv_$var
    done
    ac_cv_enable_lib_dbm=$ac_cv_enable_code_dbm
    ac_cv_enable_lib_wrap=$ac_cv_enable_code_wrap
  else
    echo "$ac_t""no" 1>&6
  fi

ac_aux_dir=
for ac_dir in $srcdir $srcdir/.. $srcdir/../..; do
  if test -f $ac_dir/install-sh; then
    ac_aux_dir=$ac_dir
    ac_install_sh="$ac_aux_dir/install-sh -c"
    break
  elif test -f $ac_dir/install.sh; then
    ac_aux_dir=$ac_dir
    ac_install_sh="$ac_aux_dir/install.sh -c"
    break
  fi
done
if test -z "$ac_aux_dir"; then
  { echo "configure: error: can not find install-sh or install.sh in $srcdir $srcdir/.. $srcdir/../.." 1>&2; exit 1; }
fi
ac_config_guess=$ac_aux_dir/config.guess
ac_config_sub=$ac_aux_dir/config.sub
ac_configure=$ac_aux_dir/configure # This should be Cygnus configure.


# Make sure we can run config.sub.
if $ac_config_sub sun4 >/dev/null 2>&1; then :
else { echo "configure: error: can not run $ac_config_sub" 1>&2; exit 1; }
fi

echo $ac_n "checking host system type""... $ac_c" 1>&6

host_alias=$host
case "$host_alias" in
NONE)
  case $nonopt in
  NONE)
    if host_alias=`$ac_config_guess`; then :
    else { echo "configure: error: can not guess host type; you must specify one" 1>&2; exit 1; }
    fi ;;
  *) host_alias=$nonopt ;;
  esac ;;
esac

host=`$ac_config_sub $host_alias`
host_cpu=`echo $host | sed 's/^\(.*\)-\(.*\)-\(.*\)$/\1/'`
host_vendor=`echo $host | sed 's/^\(.*\)-\(.*\)-\(.*\)$/\2/'`
host_os=`echo $host | sed 's/^\(.*\)-\(.*\)-\(.*\)$/\3/'`
echo "$ac_t""$host" 1>&6


if test "${CFLAGS+set}" != set; then
  CFLAGS="-O"
  ac_cflags_set=no
fi
# Extract the first word of "gcc", so it can be a program name with args.
set dummy gcc; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_CC'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test -n "$CC"; then
  ac_cv_prog_CC="$CC" # Let the user override the test.
else
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_prog_CC="gcc"
      break
    fi
  done
  IFS="$ac_save_ifs"
fi
fi
CC="$ac_cv_prog_CC"
if test -n "$CC"; then
  echo "$ac_t""$CC" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

if test -z "$CC"; then
  # Extract the first word of "cc", so it can be a program name with args.
set dummy cc; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_CC'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test -n "$CC"; then
  ac_cv_prog_CC="$CC" # Let the user override the test.
else
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  ac_prog_rejected=no
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      if test "$ac_dir/$ac_word" = "/usr/ucb/cc"; then
        ac_prog_rejected=yes
	continue
      fi
      ac_cv_prog_CC="cc"
      break
    fi
  done
  IFS="$ac_save_ifs"
if test $ac_prog_rejected = yes; then
  # We found a bogon in the path, so make sure we never use it.
  set dummy $ac_cv_prog_CC
  shift
  if test $# -gt 0; then
    # We chose a different compiler from the bogus one.
    # However, it has the same basename, so the bogon will be chosen
    # first if we set CC to just the basename; use the full file name.
    shift
    set dummy "$ac_dir/$ac_word" "$@"
    shift
    ac_cv_prog_CC="$@"
  fi
fi
fi
fi
CC="$ac_cv_prog_CC"
if test -n "$CC"; then
  echo "$ac_t""$CC" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

  test -z "$CC" && { echo "configure: error: no acceptable cc found in \$PATH" 1>&2; exit 1; }
fi

echo $ac_n "checking whether we are using GNU C""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_gcc'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.c <<EOF
#ifdef __GNUC__
  yes;
#endif
EOF
if { ac_try='${CC-cc} -E conftest.c'; { (eval echo configure:1169: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }; } | egrep yes >/dev/null 2>&1; then
  ac_cv_prog_gcc=yes
else
  ac_cv_prog_gcc=no
fi
fi

echo "$ac_t""$ac_cv_prog_gcc" 1>&6
if test $ac_cv_prog_gcc = yes; then
  GCC=yes
  if test "${CFLAGS+set}" != set; then
    echo $ac_n "checking whether ${CC-cc} accepts -g""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_gcc_g'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  echo 'void f(){}' > conftest.c
if test -z "`${CC-cc} -g -c conftest.c 2>&1`"; then
  ac_cv_prog_gcc_g=yes
else
  ac_cv_prog_gcc_g=no
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_prog_gcc_g" 1>&6
    if test $ac_cv_prog_gcc_g = yes; then
      CFLAGS="-g -O"
    else
      CFLAGS="-O"
    fi
  fi
else
  GCC=
  test "${CFLAGS+set}" = set || CFLAGS="-g"
fi

echo $ac_n "checking how to run the C preprocessor""... $ac_c" 1>&6
# On Suns, sometimes $CPP names a directory.
if test -n "$CPP" && test -d "$CPP"; then
  CPP=
fi
if test -z "$CPP"; then
if eval "test \"`echo '$''{'ac_cv_prog_CPP'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
    # This must be in double quotes, not single quotes, because CPP may get
  # substituted into the Makefile and "${CC-cc}" will confuse make.
  CPP="${CC-cc} -E"
  # On the NeXT, cc -E runs the code through the compiler's parser,
  # not just through cpp.
  cat > conftest.$ac_ext <<EOF
#line 1221 "configure"
#include "confdefs.h"
#include <assert.h>
Syntax Error
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:1227: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  :
else
  echo "$ac_err" >&5
  rm -rf conftest*
  CPP="${CC-cc} -E -traditional-cpp"
  cat > conftest.$ac_ext <<EOF
#line 1236 "configure"
#include "confdefs.h"
#include <assert.h>
Syntax Error
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:1242: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  :
else
  echo "$ac_err" >&5
  rm -rf conftest*
  CPP=/lib/cpp
fi
rm -f conftest*
fi
rm -f conftest*
  ac_cv_prog_CPP="$CPP"
fi
  CPP="$ac_cv_prog_CPP"
else
  ac_cv_prog_CPP="$CPP"
fi
echo "$ac_t""$CPP" 1>&6

echo $ac_n "checking for AIX""... $ac_c" 1>&6
cat > conftest.$ac_ext <<EOF
#line 1264 "configure"
#include "confdefs.h"
#ifdef _AIX
  yes
#endif

EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "yes" >/dev/null 2>&1; then
  rm -rf conftest*
  echo "$ac_t""yes" 1>&6; cat >> confdefs.h <<\EOF
#define _ALL_SOURCE 1
EOF

else
  rm -rf conftest*
  echo "$ac_t""no" 1>&6
fi
rm -f conftest*


echo $ac_n "checking for POSIXized ISC""... $ac_c" 1>&6
if test -d /etc/conf/kconfig.d &&
  grep _POSIX_VERSION /usr/include/sys/unistd.h >/dev/null 2>&1
then
  echo "$ac_t""yes" 1>&6
  ISC=yes # If later tests want to check for ISC.
  cat >> confdefs.h <<\EOF
#define _POSIX_SOURCE 1
EOF

  if test "$GCC" = yes; then
    CC="$CC -posix"
  else
    CC="$CC -Xp"
  fi
else
  echo "$ac_t""no" 1>&6
  ISC=
fi

ac_safe=`echo "minix/config.h" | tr './\055' '___'`
echo $ac_n "checking for minix/config.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 1311 "configure"
#include "confdefs.h"
#include <minix/config.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:1316: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  MINIX=yes
else
  echo "$ac_t""no" 1>&6
MINIX=
fi

if test "$MINIX" = yes; then
  cat >> confdefs.h <<\EOF
#define _POSIX_SOURCE 1
EOF

  cat >> confdefs.h <<\EOF
#define _POSIX_1_SOURCE 2
EOF

  cat >> confdefs.h <<\EOF
#define _MINIX 1
EOF

fi
	

if test "$target" = "NONE"; then
  target=$host
fi
echo $ac_n "checking whether 'conf/$target' exists""... $ac_c" 1>&6
if test -x conf/$target; then
    echo "$ac_t""good" 1>&6
    MMDF_TARGET=yes
else
    echo "$ac_t""no" 1>&6
    echo "configure: warning: "directory conf/$target doesn\'t exists."" 1>&2
    echo "configure: warning: cannot finish configuration with 'sitesetup $target'" 1>&2
    if test "$create_target" = "yes"; then
      
  /bin/echo "creating setup for '$target'"
  cd conf
  mkdir -p $target
  cp sample/* $target/
  cd ..

      MMDF_TARGET=yes
    else
      echo "configure: warning: Please run one of:" 1>&2
      echo "configure: warning: 'configure --target=<existing target>'" 1>&2
      echo "configure: warning: 'configure --create-target'" 1>&2
      { echo "configure: error: Cannot continue with configure until target exits" 1>&2; exit 1; }
    fi
fi

# check whether the compiler accepts -pipe









# Check whether --with-mmdfprefix or --without-mmdfprefix was given.
if test "${with_mmdfprefix+set}" = set; then
  withval="$with_mmdfprefix"
  :
fi

# Check whether --with-mmdflogin or --without-mmdflogin was given.
if test "${with_mmdflogin+set}" = set; then
  withval="$with_mmdflogin"
  :
fi

# Check whether --with-mmdfgroup or --without-mmdfgroup was given.
if test "${with_mmdfgroup+set}" = set; then
  withval="$with_mmdfgroup"
  :
fi

# Check whether --with-rootlogin or --without-rootlogin was given.
if test "${with_rootlogin+set}" = set; then
  withval="$with_rootlogin"
  :
fi


# Check whether --enable-nameserver or --disable-nameserver was given.
if test "${enable_nameserver+set}" = set; then
  enableval="$enable_nameserver"
  :
fi

# Check whether --enable-dbm or --disable-dbm was given.
if test "${enable_dbm+set}" = set; then
  enableval="$enable_dbm"
  :
fi

# Check whether --enable-wrap or --disable-wrap was given.
if test "${enable_wrap+set}" = set; then
  enableval="$enable_wrap"
  :
fi

# Check whether --enable-nis or --disable-nis was given.
if test "${enable_nis+set}" = set; then
  enableval="$enable_nis"
  :
fi

# Check whether --enable-nn or --disable-nn was given.
if test "${enable_nn+set}" = set; then
  enableval="$enable_nn"
  :
fi

# Check whether --enable-nosrcroute or --disable-nosrcroute was given.
if test "${enable_nosrcroute+set}" = set; then
  enableval="$enable_nosrcroute"
  :
fi

# Check whether --enable-8bit or --disable-8bit was given.
if test "${enable_8bit+set}" = set; then
  enableval="$enable_8bit"
  :
fi

# Check whether --enable-lock_files or --disable-lock_files was given.
if test "${enable_lock_files+set}" = set; then
  enableval="$enable_lock_files"
  :
fi

# Check whether --enable-dial or --disable-dial was given.
if test "${enable_dial+set}" = set; then
  enableval="$enable_dial"
  :
fi

# Check whether --enable-badusers or --disable-badusers was given.
if test "${enable_badusers+set}" = set; then
  enableval="$enable_badusers"
  :
fi

# Check whether --enable-bboards or --disable-bboards was given.
if test "${enable_bboards+set}" = set; then
  enableval="$enable_bboards"
  :
fi

# Check whether --enable-blockaddr or --disable-blockaddr was given.
if test "${enable_blockaddr+set}" = set; then
  enableval="$enable_blockaddr"
  :
fi

# Check whether --enable-delay or --disable-delay was given.
if test "${enable_delay+set}" = set; then
  enableval="$enable_delay"
  :
fi

# Check whether --enable-ean or --disable-ean was given.
if test "${enable_ean+set}" = set; then
  enableval="$enable_ean"
  :
fi

# Check whether --enable-list or --disable-list was given.
if test "${enable_list+set}" = set; then
  enableval="$enable_list"
  :
fi

# Check whether --enable-niftp or --disable-niftp was given.
if test "${enable_niftp+set}" = set; then
  enableval="$enable_niftp"
  :
fi

# Check whether --enable-phone or --disable-phone was given.
if test "${enable_phone+set}" = set; then
  enableval="$enable_phone"
  :
fi

# Check whether --enable-pobox or --disable-pobox was given.
if test "${enable_pobox+set}" = set; then
  enableval="$enable_pobox"
  :
fi

# Check whether --enable-pop or --disable-pop was given.
if test "${enable_pop+set}" = set; then
  enableval="$enable_pop"
  :
fi

# Check whether --enable-prog or --disable-prog was given.
if test "${enable_prog+set}" = set; then
  enableval="$enable_prog"
  :
fi

# Check whether --enable-smphone or --disable-smphone was given.
if test "${enable_smphone+set}" = set; then
  enableval="$enable_smphone"
  :
fi

# Check whether --enable-smtp or --disable-smtp was given.
if test "${enable_smtp+set}" = set; then
  enableval="$enable_smtp"
  :
fi

# Check whether --enable-uucp or --disable-uucp was given.
if test "${enable_uucp+set}" = set; then
  enableval="$enable_uucp"
  :
fi

# Check whether --enable-msg or --disable-msg was given.
if test "${enable_msg+set}" = set; then
  enableval="$enable_msg"
  :
fi

# Check whether --enable-other or --disable-other was given.
if test "${enable_other+set}" = set; then
  enableval="$enable_other"
  :
fi

# Check whether --enable-send or --disable-send was given.
if test "${enable_send+set}" = set; then
  enableval="$enable_send"
  :
fi

# Check whether --enable-snd or --disable-snd was given.
if test "${enable_snd+set}" = set; then
  enableval="$enable_snd"
  :
fi

# Check whether --enable-ucbmail or --disable-ucbmail was given.
if test "${enable_ucbmail+set}" = set; then
  enableval="$enable_ucbmail"
  :
fi

# Check whether --enable-unsupported or --disable-unsupported was given.
if test "${enable_unsupported+set}" = set; then
  enableval="$enable_unsupported"
  :
fi

# Check whether --enable-gcc or --disable-gcc was given.
if test "${enable_gcc+set}" = set; then
  enableval="$enable_gcc"
  :
fi

# Check whether --enable-fhs or --disable-fhs was given.
if test "${enable_fhs+set}" = set; then
  enableval="$enable_fhs"
  :
fi

# Check whether --enable-elf or --disable-elf was given.
if test "${enable_elf+set}" = set; then
  enableval="$enable_elf"
  :
fi

# Check whether --enable-esmtp or --disable-esmtp was given.
if test "${enable_esmtp+set}" = set; then
  enableval="$enable_esmtp"
  :
fi

# Check whether --enable-esmtp_dsn or --disable-esmtp_dsn was given.
if test "${enable_esmtp_dsn+set}" = set; then
  enableval="$enable_esmtp_dsn"
  :
fi

# Check whether --enable-esmtp_8bitmime or --disable-esmtp_8bitmime was given.
if test "${enable_esmtp_8bitmime+set}" = set; then
  enableval="$enable_esmtp_8bitmime"
  :
fi

# Check whether --enable-wildcard or --disable-wildcard was given.
if test "${enable_wildcard+set}" = set; then
  enableval="$enable_wildcard"
  :
fi

# Check whether --enable-rbl or --disable-rbl was given.
if test "${enable_rbl+set}" = set; then
  enableval="$enable_rbl"
  :
fi

# Check whether --enable-filelist or --disable-filelist was given.
if test "${enable_filelist+set}" = set; then
  enableval="$enable_filelist"
  :
fi



if test "$enable_gcc" = "no"; then
  ac_cv_prog_CC=cc
  ac_cv_prog_gcc=no
  GCC=
  CC=cc
  echo "using ${CC-cc}"
  PIPE=""
else
  # Extract the first word of "gcc", so it can be a program name with args.
set dummy gcc; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_PATH_GCC'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$PATH_GCC" in
  /*)
  ac_cv_path_PATH_GCC="$PATH_GCC" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_PATH_GCC="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
PATH_GCC="$ac_cv_path_PATH_GCC"
if test -n "$PATH_GCC"; then
  echo "$ac_t""$PATH_GCC" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

  
echo $ac_n "checking whether compiler accepts -pipe""... $ac_c" 1>&6
temp_cflags=${CFLAGS}
CFLAGS="${CFLAGS} -pipe"

  if eval "test \"`echo '$''{'ac_cv_cc_have_pipe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  
    cat > conftest.$ac_ext <<EOF
#line 1693 "configure"
#include "confdefs.h"

int main() { return 0; }
int t() {

; return 0; }
EOF
if { (eval echo configure:1701: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_cc_have_pipe=yes
else
  rm -rf conftest*
  ac_cv_cc_have_pipe=no
fi
rm -f conftest*

fi

echo "$ac_t""${ac_cv_cc_have_pipe}" 1>&6
CFLAGS=${temp_cflags}
if test "${ac_cv_cc_have_pipe}" != yes; then
  PIPE=
else
  PIPE="-pipe"
fi

fi
  


echo "OS=$host_os"
case $host_os in
   sco*)
      if test "$enable_elf" != no; then
         # check whether the compiler accepts -b elf or -m elf
         echo $ac_n "checking whether compiler compiler flags for elf""... $ac_c" 1>&6
         temp_cflags=${CFLAGS}
       CFLAGS="${CFLAGS} -m elf"
         if eval "test \"`echo '$''{'ac_cv_elf_flag'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 1736 "configure"
#include "confdefs.h"

int main() { return 0; }
int t() {

; return 0; }
EOF
if { (eval echo configure:1744: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_elf_flag="-m elf"
else
  rm -rf conftest*
  cat > conftest.$ac_ext <<EOF
#line 1750 "configure"
#include "confdefs.h"

int main() { return 0; }
int t() {

; return 0; }
EOF
if { (eval echo configure:1758: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_elf_flag="-b elf"
else
  rm -rf conftest*
  ac_cv_elf_flag=""
fi
rm -f conftest*

fi
rm -f conftest*

       
fi

       CFLAGS=${temp_cflags}
       if test "${ac_cv_elf_flag}" != ""; then
          echo "$ac_t""${ac_cv_elf_flag}" 1>&6
          ELF="${ac_cv_elf_flag=}"
       else
          echo "$ac_t""not set" 1>&6
          ELF=
       fi
       
      fi
   ;;
   hpux9.*)
      MYDEFS="${MYDEFS} -D_BSD"
      cat >> confdefs.h <<EOF
#define HPUX 1
EOF

   ;;
   hpux10.*)
      MYDEFS="${MYDEFS} -D_BSD"
      cat >> confdefs.h <<EOF
#define HPUX 1
EOF

   ;;
   linux*)
      cat >> confdefs.h <<EOF
#define LINUX 1
EOF

   ;;
esac

echo "CFLAGS="$CFLAGS

echo $ac_n "checking whether _AIX is defined""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_is_aix'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 1813 "configure"
#include "confdefs.h"
#ifdef _AIX
    yes
#endif

EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "yes" >/dev/null 2>&1; then
  rm -rf conftest*
  ac_cv_is_aix=yes
else
  rm -rf conftest*
  ac_cv_is_aix=no
fi
rm -f conftest*

fi

if test "$ac_cv_is_aix" = yes; then
  echo "$ac_t""yes" 1>&6
else
  echo "$ac_t""no" 1>&6
fi


# getmntent is in -lsun on Irix 4, -lseq on Dynix/PTX, -lgen on Unixware.
echo $ac_n "checking for -lsun""... $ac_c" 1>&6
ac_lib_var=`echo sun'_'getmntent | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lsun  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 1848 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char getmntent();

int main() { return 0; }
int t() {
getmntent()
; return 0; }
EOF
if { (eval echo configure:1860: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="-lsun $LIBS"
else
  echo "$ac_t""no" 1>&6
echo $ac_n "checking for -lseq""... $ac_c" 1>&6
ac_lib_var=`echo seq'_'getmntent | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lseq  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 1884 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char getmntent();

int main() { return 0; }
int t() {
getmntent()
; return 0; }
EOF
if { (eval echo configure:1896: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="-lseq $LIBS"
else
  echo "$ac_t""no" 1>&6
echo $ac_n "checking for -lgen""... $ac_c" 1>&6
ac_lib_var=`echo gen'_'getmntent | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lgen  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 1920 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char getmntent();

int main() { return 0; }
int t() {
getmntent()
; return 0; }
EOF
if { (eval echo configure:1932: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="-lgen $LIBS"
else
  echo "$ac_t""no" 1>&6
fi

fi

fi

echo $ac_n "checking for getmntent""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_getmntent'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 1959 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char getmntent(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char getmntent();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_getmntent) || defined (__stub___getmntent)
choke me
#else
getmntent();
#endif

; return 0; }
EOF
if { (eval echo configure:1983: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_getmntent=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_getmntent=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'getmntent`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_GETMNTENT 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

# strftime is in -lintl on SCO UNIX.
echo $ac_n "checking for -lintl""... $ac_c" 1>&6
ac_lib_var=`echo intl'_'strftime | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lintl  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 2012 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char strftime();

int main() { return 0; }
int t() {
strftime()
; return 0; }
EOF
if { (eval echo configure:2024: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="-lintl $LIBS"
else
  echo "$ac_t""no" 1>&6
fi

echo $ac_n "checking for strftime""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_strftime'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 2047 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char strftime(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char strftime();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_strftime) || defined (__stub___strftime)
choke me
#else
strftime();
#endif

; return 0; }
EOF
if { (eval echo configure:2071: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_strftime=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_strftime=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'strftime`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_STRFTIME 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

ac_header_dirent=no
for ac_hdr in dirent.h sys/ndir.h sys/dir.h ndir.h
do
ac_safe=`echo "$ac_hdr" | tr './\055' '___'`
echo $ac_n "checking for $ac_hdr that defines DIR""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_dirent_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 2100 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <$ac_hdr>
int main() { return 0; }
int t() {
DIR *dirp = 0;
; return 0; }
EOF
if { (eval echo configure:2109: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_header_dirent_$ac_safe=yes"
else
  rm -rf conftest*
  eval "ac_cv_header_dirent_$ac_safe=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_header_dirent_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_hdr=HAVE_`echo $ac_hdr | tr 'abcdedfghijklmnopqrstuvwxyz./\055' 'ABCDEDFGHIJKLMNOPQRSTUVWXYZ___'`
  cat >> confdefs.h <<EOF
#define $ac_tr_hdr 1
EOF
 ac_header_dirent=$ac_hdr; break
else
  echo "$ac_t""no" 1>&6
fi
done
# Two versions of opendir et al. are in -ldir and -lx on SCO Xenix.
if test $ac_header_dirent = dirent.h; then
echo $ac_n "checking for -ldir""... $ac_c" 1>&6
ac_lib_var=`echo dir'_'opendir | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ldir  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 2140 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char opendir();

int main() { return 0; }
int t() {
opendir()
; return 0; }
EOF
if { (eval echo configure:2152: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="$LIBS -ldir"
else
  echo "$ac_t""no" 1>&6
fi

else
echo $ac_n "checking for -lx""... $ac_c" 1>&6
ac_lib_var=`echo x'_'opendir | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lx  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 2179 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char opendir();

int main() { return 0; }
int t() {
opendir()
; return 0; }
EOF
if { (eval echo configure:2191: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="$LIBS -lx"
else
  echo "$ac_t""no" 1>&6
fi

fi



  OLDCFLAGS=$CFLAGS
  WHAT_DEPEND="depend-M"
  echo $ac_n "checking for $CC support -M""... $ac_c" 1>&6
  CFLAGS=$OLDCFLAGS" -M"
  cat > conftest.$ac_ext <<EOF
#line 2218 "configure"
#include "confdefs.h"
#include <stdio.h>
int main() { return 0; }
int t() {

; return 0; }
EOF
result=`eval $ac_compile 2>&1 `
if test "$result" = ""; then
  ac_cv_depend_flag=yes
else
  output=`echo $result |grep stdio.h`
  if test "$output" = ""; then
    ac_cv_depend_flag=no
  else
    ac_cv_depend_flag=yes
  fi
fi
rm -f conftest*

  if test "$ac_cv_depend_flag" = yes; then
     echo "$ac_t""yes" 1>&6
     DEPEND_FLAG="-M"
  else
     echo "$ac_t""no" 1>&6
     echo $ac_n "checking for $CC support -Em""... $ac_c" 1>&6
     CFLAGS=$OLDCFLAGS" -Em"
          cat > conftest.$ac_ext <<EOF
#line 2247 "configure"
#include "confdefs.h"
#include <stdio.h>
int main() { return 0; }
int t() {
int depend_flag
; return 0; }
EOF
result=`eval $ac_compile 2>&1 `
if test "$result" = ""; then
  ac_cv_depend_flag=yes
else
  output=`echo $result |grep stdio.h`
  if test "$output" = ""; then
    ac_cv_depend_flag=no
  else
    ac_cv_depend_flag=yes
  fi
fi
rm -f conftest*

     if test "$ac_cv_depend_flag" = yes; then
        echo "$ac_t""yes" 1>&6
        DEPEND_FLAG="-Em"
     else
        echo "$ac_t""no" 1>&6
        echo $ac_n "checking for $CC support -E -H""... $ac_c" 1>&6
        CFLAGS=$OLDCFLAGS" -E -H"
        cat > conftest.$ac_ext <<EOF
#line 2276 "configure"
#include "confdefs.h"
#include <stdio.h>
int main() { return 0; }
int t() {

; return 0; }
EOF
result=`eval $ac_compile 2>&1 `
if test "$result" = ""; then
  ac_cv_depend_flag=yes
else
  output=`echo $result |grep stdio.h`
  if test "$output" = ""; then
    ac_cv_depend_flag=no
  else
    ac_cv_depend_flag=yes
  fi
fi
rm -f conftest*

        if test "$ac_cv_depend_flag" = yes; then
           echo "$ac_t""yes" 1>&6
           DEPEND_FLAG="-E -H"
        else
           echo "$ac_t""no" 1>&6
           echo $ac_n "checking for $CC support -E""... $ac_c" 1>&6
           CFLAGS=$OLDCFLAGS" -E"
           cat > conftest.$ac_ext <<EOF
#line 2305 "configure"
#include "confdefs.h"
#include <stdio.h>
int main() { return 0; }
int t() {

; return 0; }
EOF
result=`eval $ac_compile 2>&1 `
if test "$result" = ""; then
  ac_cv_depend_flag=yes
else
  output=`echo $result |grep stdio.h`
  if test "$output" = ""; then
    ac_cv_depend_flag=no
  else
    ac_cv_depend_flag=yes
  fi
fi
rm -f conftest*

           if test "$ac_cv_depend_flag" = yes; then
              echo "$ac_t""yes" 1>&6
              DEPEND_FLAG="-E"
           else
              echo "$ac_t""no" 1>&6
              DEPEND_FLAG=""
	      WHAT_DEPEND="depend-cat"
           fi
        fi
     fi
  fi
  CFLAGS=$OLDCFLAGS

for ac_prog in mawk gawk nawk awk
do
# Extract the first word of "$ac_prog", so it can be a program name with args.
set dummy $ac_prog; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_AWK'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test -n "$AWK"; then
  ac_cv_prog_AWK="$AWK" # Let the user override the test.
else
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_prog_AWK="$ac_prog"
      break
    fi
  done
  IFS="$ac_save_ifs"
fi
fi
AWK="$ac_cv_prog_AWK"
if test -n "$AWK"; then
  echo "$ac_t""$AWK" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

test -n "$AWK" && break
done

# Find a good install program.  We prefer a C program (faster),
# so one script is as good as another.  But avoid the broken or
# incompatible versions:
# SysV /etc/install, /usr/sbin/install
# SunOS /usr/etc/install
# IRIX /sbin/install
# AIX /bin/install
# AFS /usr/afsws/bin/install, which mishandles nonexistent args
# SVR4 /usr/ucb/install, which tries to use the nonexistent group "staff"
# ./install, which can be erroneously created by make from ./install.sh.
echo $ac_n "checking for a BSD compatible install""... $ac_c" 1>&6
if test -z "$INSTALL"; then
if eval "test \"`echo '$''{'ac_cv_path_install'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
    IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    # Account for people who put trailing slashes in PATH elements.
    case "$ac_dir/" in
    /|./|.//|/etc/*|/usr/sbin/*|/usr/etc/*|/sbin/*|/usr/afsws/bin/*|/usr/ucb/*) ;;
    *)
      # OSF1 and SCO ODT 3.0 have their own names for install.
      for ac_prog in ginstall installbsd scoinst install; do
        if test -f $ac_dir/$ac_prog; then
	  if test $ac_prog = install &&
            grep dspmsg $ac_dir/$ac_prog >/dev/null 2>&1; then
	    # AIX install.  It has an incompatible calling convention.
	    # OSF/1 installbsd also uses dspmsg, but is usable.
	    :
	  else
	    ac_cv_path_install="$ac_dir/$ac_prog -c"
	    break 2
	  fi
	fi
      done
      ;;
    esac
  done
  IFS="$ac_save_ifs"

fi
  if test "${ac_cv_path_install+set}" = set; then
    INSTALL="$ac_cv_path_install"
  else
    # As a last resort, use the slow shell script.  We don't cache a
    # path for INSTALL within a source directory, because that will
    # break other packages using the cache if that directory is
    # removed, or if the path is relative.
    INSTALL="$ac_install_sh"
  fi
fi
echo "$ac_t""$INSTALL" 1>&6

# Use test -z because SunOS4 sh mishandles braces in ${var-val}.
# It thinks the first close brace ends the variable substitution.
test -z "$INSTALL_PROGRAM" && INSTALL_PROGRAM='${INSTALL}'

test -z "$INSTALL_DATA" && INSTALL_DATA='${INSTALL} -m 644'

echo $ac_n "checking whether ln -s works""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_LN_S'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  rm -f conftestdata
if ln -s X conftestdata 2>/dev/null
then
  rm -f conftestdata
  ac_cv_prog_LN_S="ln -s"
else
  ac_cv_prog_LN_S=ln
fi
fi
LN_S="$ac_cv_prog_LN_S"
if test "$ac_cv_prog_LN_S" = "ln -s"; then
  echo "$ac_t""yes" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

echo $ac_n "checking whether ${MAKE-make} sets \${MAKE}""... $ac_c" 1>&6
set dummy ${MAKE-make}; ac_make=$2
if eval "test \"`echo '$''{'ac_cv_prog_make_${ac_make}_set'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftestmake <<\EOF
all:
	@echo 'ac_maketemp="${MAKE}"'
EOF
# GNU make sometimes prints "make[1]: Entering...", which would confuse us.
eval `${MAKE-make} -f conftestmake 2>/dev/null | grep temp=`
if test -n "$ac_maketemp"; then
  eval ac_cv_prog_make_${ac_make}_set=yes
else
  eval ac_cv_prog_make_${ac_make}_set=no
fi
rm -f conftestmake
fi
if eval "test \"`echo '$ac_cv_prog_make_'${ac_make}_set`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  SET_MAKE=
else
  echo "$ac_t""no" 1>&6
  SET_MAKE="MAKE=${MAKE-make}"
fi

# Extract the first word of "ar", so it can be a program name with args.
set dummy ar; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_AR'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$AR" in
  /*)
  ac_cv_path_AR="$AR" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_AR="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
AR="$ac_cv_path_AR"
if test -n "$AR"; then
  echo "$ac_t""$AR" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

# Extract the first word of "xstr", so it can be a program name with args.
set dummy xstr; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_XSTR'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$XSTR" in
  /*)
  ac_cv_path_XSTR="$XSTR" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_XSTR="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
XSTR="$ac_cv_path_XSTR"
if test -n "$XSTR"; then
  echo "$ac_t""$XSTR" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

# Extract the first word of "ctags", so it can be a program name with args.
set dummy ctags; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_CTAGS'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$CTAGS" in
  /*)
  ac_cv_path_CTAGS="$CTAGS" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_CTAGS="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
CTAGS="$ac_cv_path_CTAGS"
if test -n "$CTAGS"; then
  echo "$ac_t""$CTAGS" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

# Extract the first word of "dial", so it can be a program name with args.
set dummy dial; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_D_DIALPROG'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$D_DIALPROG" in
  /*)
  ac_cv_path_D_DIALPROG="$D_DIALPROG" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_D_DIALPROG="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
D_DIALPROG="$ac_cv_path_D_DIALPROG"
if test -n "$D_DIALPROG"; then
  echo "$ac_t""$D_DIALPROG" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

# Extract the first word of "assign", so it can be a program name with args.
set dummy assign; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_ASSIGN'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test -n "$ASSIGN"; then
  ac_cv_prog_ASSIGN="$ASSIGN" # Let the user override the test.
else
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_prog_ASSIGN="assign"
      break
    fi
  done
  IFS="$ac_save_ifs"
fi
fi
ASSIGN="$ac_cv_prog_ASSIGN"
if test -n "$ASSIGN"; then
  echo "$ac_t""$ASSIGN" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

if test "$ac_cv_prog_ASSIGN" != ""; then
    cat >> confdefs.h <<\EOF
#define HAVE_ASSIGN 1
EOF

fi
# Extract the first word of "chown", so it can be a program name with args.
set dummy chown; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_CHOWN'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$CHOWN" in
  /*)
  ac_cv_path_CHOWN="$CHOWN" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_CHOWN="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
CHOWN="$ac_cv_path_CHOWN"
if test -n "$CHOWN"; then
  echo "$ac_t""$CHOWN" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

# Extract the first word of "lorder", so it can be a program name with args.
set dummy lorder; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_LORDER'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test -n "$LORDER"; then
  ac_cv_prog_LORDER="$LORDER" # Let the user override the test.
else
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_prog_LORDER="lorder"
      break
    fi
  done
  IFS="$ac_save_ifs"
fi
fi
LORDER="$ac_cv_prog_LORDER"
if test -n "$LORDER"; then
  echo "$ac_t""$LORDER" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

if test "$ac_cv_prog_LORDER" != ""; then
    LIBFIX="./libfix"
else
    LIBFIX="./libfix.linux"
fi
# Extract the first word of "print", so it can be a program name with args.
set dummy print; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_VPRINT'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$VPRINT" in
  /*)
  ac_cv_path_VPRINT="$VPRINT" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_VPRINT="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
VPRINT="$ac_cv_path_VPRINT"
if test -n "$VPRINT"; then
  echo "$ac_t""$VPRINT" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

if test "$ac_cv_prog_VPRINT" = ""; then
  # Extract the first word of "printf", so it can be a program name with args.
set dummy printf; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_path_VPRINT'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  case "$VPRINT" in
  /*)
  ac_cv_path_VPRINT="$VPRINT" # Let the user override the test with a path.
  ;;
  *)
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_path_VPRINT="$ac_dir/$ac_word"
      break
    fi
  done
  IFS="$ac_save_ifs"
  ;;
esac
fi
VPRINT="$ac_cv_path_VPRINT"
if test -n "$VPRINT"; then
  echo "$ac_t""$VPRINT" 1>&6
else
  echo "$ac_t""no" 1>&6
fi

fi
# Extract the first word of "ranlib", so it can be a program name with args.
set dummy ranlib; ac_word=$2
echo $ac_n "checking for $ac_word""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_RANLIB'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test -n "$RANLIB"; then
  ac_cv_prog_RANLIB="$RANLIB" # Let the user override the test.
else
  IFS="${IFS= 	}"; ac_save_ifs="$IFS"; IFS="${IFS}:"
  for ac_dir in $PATH; do
    test -z "$ac_dir" && ac_dir=.
    if test -f $ac_dir/$ac_word; then
      ac_cv_prog_RANLIB="ranlib"
      break
    fi
  done
  IFS="$ac_save_ifs"
  test -z "$ac_cv_prog_RANLIB" && ac_cv_prog_RANLIB=":"
fi
fi
RANLIB="$ac_cv_prog_RANLIB"
if test -n "$RANLIB"; then
  echo "$ac_t""$RANLIB" 1>&6
else
  echo "$ac_t""no" 1>&6
fi



  echo $ac_n "checking for file /etc/securetty""... $ac_c" 1>&6

  if test -f "/etc/securetty"; then
    echo "$ac_t""yes" 1>&6
    cat >> confdefs.h <<EOF
#define HAVE_SECURETTY 1
EOF

  else
    echo "$ac_t""no" 1>&6
      fi


  echo $ac_n "checking for file /etc/securetty-test""... $ac_c" 1>&6

  if test -f "/etc/securetty-test"; then
    echo "$ac_t""yes" 1>&6
    cat >> confdefs.h <<EOF
#define HAVE_TSECURETTY 1
EOF

  else
    echo "$ac_t""no" 1>&6
      fi


if test "$libdir" = '${exec_prefix}/lib'; then
  if test "x$prefix" = xNONE; then
    if test "x$exec_prefix" != xNONE; then
      LDFLAGS="-L${exec_prefix}/lib ${LDFLAGS}"
      ac_configure_args="$ac_configure_args --libdir=${exec_prefix}/lib"
    else
      LDFLAGS="-L${ac_default_prefix}/local/lib ${LDFLAGS}"
      ac_configure_args="$ac_configure_args --libdir=${ac_default_prefix}/local/lib"
    fi
  else
    LDFLAGS="-L${prefix}/lib ${LDFLAGS}"
    ac_configure_args="$ac_configure_args --libdir=${prefix}/lib"
  fi
else
  if test "x$libdir" != xNONE/lib; then
    LDFLAGS="-L${libdir} ${LDFLAGS}"
  fi
fi

if test "$includedir" = '${prefix}/include'; then
  if test "x$prefix" != xNONE && test "x$prefix" != "x/usr"; then
    CPPFLAGS="-I${prefix}/include ${CPPFLAGS}"
    CFLAGS="-I${prefix}/include ${CFLAGS}"
    ac_configure_args="$ac_configure_args --includedir=${prefix}/include"
  fi
else
  CPPFLAGS="-I${includedir} ${CPPFLAGS}"
  CFLAGS="-I${includedir} ${CFLAGS}"
fi
echo "===========>$oldincludedir"

if test "x$prefix" = xNONE; then
  ac_configure_args="$ac_configure_args --prefix=$ac_default_prefix"
fi

if test "${GXX}" = "yes" && test "${ac_cxx_set}" = no; then
  CXX=gcc
fi  

echo $ac_n "checking for -ltermcap""... $ac_c" 1>&6
ac_lib_var=`echo termcap'_'tgetent | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ltermcap  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 2856 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char tgetent();

int main() { return 0; }
int t() {
tgetent()
; return 0; }
EOF
if { (eval echo configure:2868: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo termcap | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-ltermcap $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

echo $ac_n "checking for -lsocket""... $ac_c" 1>&6
ac_lib_var=`echo socket'_'socket | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lsocket  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 2900 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char socket();

int main() { return 0; }
int t() {
socket()
; return 0; }
EOF
if { (eval echo configure:2912: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo socket | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lsocket $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

echo $ac_n "checking for -lnsl""... $ac_c" 1>&6
ac_lib_var=`echo nsl'_'inet_ntoa | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lnsl  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 2944 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char inet_ntoa();

int main() { return 0; }
int t() {
inet_ntoa()
; return 0; }
EOF
if { (eval echo configure:2956: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo nsl | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lnsl $LIBS"

else
  echo "$ac_t""no" 1>&6
fi


if test "$ac_cv_enable_code_dbm" != "no"; then
   echo $ac_n "checking for dbminit""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_dbminit'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 2987 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char dbminit(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dbminit();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_dbminit) || defined (__stub___dbminit)
choke me
#else
dbminit();
#endif

; return 0; }
EOF
if { (eval echo configure:3011: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_dbminit=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_dbminit=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'dbminit`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  
      ac_safe=`echo "ndbm.h" | tr './\055' '___'`
echo $ac_n "checking for ndbm.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3030 "configure"
#include "confdefs.h"
#include <ndbm.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:3035: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  ac_cv_lib_dbm="yes"
else
  echo "$ac_t""no" 1>&6

      echo "configure: warning: Grrr. you have libndbm. But no ndbm.h file. courious" 1>&2
      echo $ac_n "checking for -ldbm""... $ac_c" 1>&6
ac_lib_var=`echo dbm'_'dbminit | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ldbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3062 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dbminit();

int main() { return 0; }
int t() {
dbminit()
; return 0; }
EOF
if { (eval echo configure:3074: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo dbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-ldbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

      if test "$ac_cv_lib_dbm_dbminit" = "no"; then
	 echo $ac_n "checking for -lgdbm""... $ac_c" 1>&6
ac_lib_var=`echo gdbm'_'gdbm_open | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lgdbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3107 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char gdbm_open();

int main() { return 0; }
int t() {
gdbm_open()
; return 0; }
EOF
if { (eval echo configure:3119: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo gdbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lgdbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

         if test "$ac_cv_lib_gdbm_gdbm_open" = "no"; then
            echo $ac_n "checking for -lndbm""... $ac_c" 1>&6
ac_lib_var=`echo ndbm'_'dbm_open | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lndbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3152 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dbm_open();

int main() { return 0; }
int t() {
dbm_open()
; return 0; }
EOF
if { (eval echo configure:3164: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo ndbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lndbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

            if test "$ac_cv_lib_ndbm_gdbm_open" = "no"; then
               echo $ac_n "checking for -ldbm""... $ac_c" 1>&6
ac_lib_var=`echo dbm'_'dbminit | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ldbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3197 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dbminit();

int main() { return 0; }
int t() {
dbminit()
; return 0; }
EOF
if { (eval echo configure:3209: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo dbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-ldbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

            fi
         fi
      fi
      
fi

else
  echo "$ac_t""no" 1>&6

      echo $ac_n "checking for -ldbm""... $ac_c" 1>&6
ac_lib_var=`echo dbm'_'dbminit | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ldbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3250 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dbminit();

int main() { return 0; }
int t() {
dbminit()
; return 0; }
EOF
if { (eval echo configure:3262: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo dbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-ldbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

      if test "$ac_cv_lib_dbm_dbminit" = "no"; then
	 echo $ac_n "checking for -lgdbm""... $ac_c" 1>&6
ac_lib_var=`echo gdbm'_'gdbm_open | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lgdbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3295 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char gdbm_open();

int main() { return 0; }
int t() {
gdbm_open()
; return 0; }
EOF
if { (eval echo configure:3307: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo gdbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lgdbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

         if test "$ac_cv_lib_gdbm_gdbm_open" = "no"; then
            echo $ac_n "checking for -lndbm""... $ac_c" 1>&6
ac_lib_var=`echo ndbm'_'dbm_open | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lndbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3340 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dbm_open();

int main() { return 0; }
int t() {
dbm_open()
; return 0; }
EOF
if { (eval echo configure:3352: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo ndbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lndbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

            if test "$ac_cv_lib_ndbm_gdbm_open" = "no"; then
               echo $ac_n "checking for -ldbm""... $ac_c" 1>&6
ac_lib_var=`echo dbm'_'dbminit | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ldbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3385 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dbminit();

int main() { return 0; }
int t() {
dbminit()
; return 0; }
EOF
if { (eval echo configure:3397: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo dbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-ldbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

            fi
         fi
      else
	 echo $ac_n "checking for -ldbm""... $ac_c" 1>&6
ac_lib_var=`echo dbm'_'gdbm_open | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ldbm  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3432 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char gdbm_open();

int main() { return 0; }
int t() {
gdbm_open()
; return 0; }
EOF
if { (eval echo configure:3444: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo dbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-ldbm $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

         if test "$ac_cv_lib_dbm_gdbm_open" = "yes"; then
	   cat >> confdefs.h <<\EOF
#define HAVE_LIBGDBM 1
EOF

	   echo "configure: warning: *** your libdbm is linked to libgdbm!" 1>&2
         else
	   echo "$ac_t""libdbm is not linked to libgdbm. Good!" 1>&6
         fi
      fi
      if test "$ac_cv_lib_gdbm_gdbm_open" = "yes" || 
	   test "$ac_cv_lib_ndbm_dbm_open" = "yes" ||
	   test "$ac_cv_lib_dbm_dbminit" = "yes" ||
	   test "$ac_cv_lib_dbm_dbm_open" = "yes"; then
         ac_cv_lib_dbm="yes"
      else
         ac_cv_lib_dbm="no"
      fi
fi

fi

if test "$ac_cv_enable_code_wrap" != "no"; then
   echo $ac_n "checking for -lwrap""... $ac_c" 1>&6
ac_lib_var=`echo wrap'_'eval_client | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lwrap  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3499 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char eval_client();

int main() { return 0; }
int t() {
eval_client()
; return 0; }
EOF
if { (eval echo configure:3511: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo wrap | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lwrap $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

   if test "$ac_cv_lib_wrap_eval_client" = "yes"; then
      ac_safe=`echo "tcpd.h" | tr './\055' '___'`
echo $ac_n "checking for tcpd.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3542 "configure"
#include "confdefs.h"
#include <tcpd.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:3547: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  echo TCPD yes
else
  echo "$ac_t""no" 1>&6

        CPPFLAGS=$CPPFLAGS" -I/usr/local/include"
        unset ac_cv_header_tcpd_h
        ac_safe=`echo "tcpd.h" | tr './\055' '___'`
echo $ac_n "checking for tcpd.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3573 "configure"
#include "confdefs.h"
#include <tcpd.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:3578: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  CFLAGS=$CFLAGS" -I/usr/local/include"
else
  echo "$ac_t""no" 1>&6
ac_cv_enable_code_wrap="no"
fi

      
fi

   fi
fi

# If we cannot run a trivial program, we must be cross compiling.
echo $ac_n "checking whether cross-compiling""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_c_cross'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_c_cross=yes
else
cat > conftest.$ac_ext <<EOF
#line 3613 "configure"
#include "confdefs.h"
main(){return(0);}
EOF
{ (eval echo configure:3617: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_c_cross=no
else
  ac_cv_c_cross=yes
fi
fi
rm -fr conftest*
fi

echo "$ac_t""$ac_cv_c_cross" 1>&6
cross_compiling=$ac_cv_c_cross

echo $ac_n "checking for ANSI C header files""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_stdc'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3635 "configure"
#include "confdefs.h"
#include <stdlib.h>
#include <stdarg.h>
#include <string.h>
#include <float.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:3643: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  ac_cv_header_stdc=yes
else
  echo "$ac_err" >&5
  rm -rf conftest*
  ac_cv_header_stdc=no
fi
rm -f conftest*

if test $ac_cv_header_stdc = yes; then
  # SunOS 4.x string.h does not declare mem*, contrary to ANSI.
cat > conftest.$ac_ext <<EOF
#line 3658 "configure"
#include "confdefs.h"
#include <string.h>
EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "memchr" >/dev/null 2>&1; then
  :
else
  rm -rf conftest*
  ac_cv_header_stdc=no
fi
rm -f conftest*

fi

if test $ac_cv_header_stdc = yes; then
  # ISC 2.0.2 stdlib.h does not declare free, contrary to ANSI.
cat > conftest.$ac_ext <<EOF
#line 3676 "configure"
#include "confdefs.h"
#include <stdlib.h>
EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "free" >/dev/null 2>&1; then
  :
else
  rm -rf conftest*
  ac_cv_header_stdc=no
fi
rm -f conftest*

fi

if test $ac_cv_header_stdc = yes; then
  # /bin/cc in Irix-4.0.5 gets non-ANSI ctype macros unless using -ansi.
if test "$cross_compiling" = yes; then
  :
else
cat > conftest.$ac_ext <<EOF
#line 3697 "configure"
#include "confdefs.h"
#include <ctype.h>
#define ISLOWER(c) ('a' <= (c) && (c) <= 'z')
#define TOUPPER(c) (ISLOWER(c) ? 'A' + ((c) - 'a') : (c))
#define XOR(e, f) (((e) && !(f)) || (!(e) && (f)))
int main () { int i; for (i = 0; i < 256; i++)
if (XOR (islower (i), ISLOWER (i)) || toupper (i) != TOUPPER (i)) exit(2);
exit (0); }

EOF
{ (eval echo configure:3708: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  :
else
  ac_cv_header_stdc=no
fi
fi
rm -fr conftest*
fi
fi

echo "$ac_t""$ac_cv_header_stdc" 1>&6
if test $ac_cv_header_stdc = yes; then
  cat >> confdefs.h <<\EOF
#define STDC_HEADERS 1
EOF

fi

ac_safe=`echo "fcntl.h" | tr './\055' '___'`
echo $ac_n "checking for fcntl.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3733 "configure"
#include "confdefs.h"
#include <fcntl.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:3738: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  
ac_cv_header_fcntl_h="yes"
cat >> confdefs.h <<EOF
#define HAVE_FCNTL_H 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

if test "$enable_lock_files" != "yes" && test "$ac_cv_header_fcntl_h" = "yes"; then
   echo $ac_n "checking for flock""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_flock'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3768 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char flock(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char flock();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_flock) || defined (__stub___flock)
choke me
#else
flock();
#endif

; return 0; }
EOF
if { (eval echo configure:3792: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_flock=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_flock=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'flock`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  ac_cv_flock="yes"
else
  echo "$ac_t""no" 1>&6


fi

   if test "$ac_cv_flock" = "yes"; then
      cat >> confdefs.h <<EOF
#define HAVE_FLOCK 1
EOF

   else
      echo $ac_n "checking whether have F_SETLK""... $ac_c" 1>&6
      if eval "test \"`echo '$''{'ac_cv_have_F_SETLK'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_F_SETLK=no
else
cat > conftest.$ac_ext <<EOF
#line 3825 "configure"
#include "confdefs.h"
#include <fcntl.h>
        main() { 
#		ifdef F_SETLK
			exit(0);
#		else
			exit(1);
#		endif
 }
EOF
{ (eval echo configure:3836: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_F_SETLK=yes
else
  ac_cv_have_F_SETLK=no
fi
fi
rm -fr conftest*
fi

      if test "$ac_cv_have_F_SETLK" = "yes"; then
         echo "$ac_t""yes" 1>&6
         cat >> confdefs.h <<EOF
#define HAVE_F_SETLK 1
EOF

      else
         echo "$ac_t""no" 1>&6
         echo $ac_n "checking for lock-style""... $ac_c" 1>&6
         cat >> confdefs.h <<EOF
#define HAVE_LOCK_FILES 1
EOF

         echo "$ac_t""lockfiles" 1>&6
      fi
   fi
else
   echo $ac_n "checking for lock-style""... $ac_c" 1>&6
   cat >> confdefs.h <<EOF
#define HAVE_LOCK_FILES 1
EOF

   echo "$ac_t""lockfiles" 1>&6
fi


ac_header_dirent=no
for ac_hdr in dirent.h sys/ndir.h sys/dir.h ndir.h
do
ac_safe=`echo "$ac_hdr" | tr './\055' '___'`
echo $ac_n "checking for $ac_hdr that defines DIR""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_dirent_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3881 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <$ac_hdr>
int main() { return 0; }
int t() {
DIR *dirp = 0;
; return 0; }
EOF
if { (eval echo configure:3890: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_header_dirent_$ac_safe=yes"
else
  rm -rf conftest*
  eval "ac_cv_header_dirent_$ac_safe=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_header_dirent_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_hdr=HAVE_`echo $ac_hdr | tr 'abcdedfghijklmnopqrstuvwxyz./\055' 'ABCDEDFGHIJKLMNOPQRSTUVWXYZ___'`
  cat >> confdefs.h <<EOF
#define $ac_tr_hdr 1
EOF
 ac_header_dirent=$ac_hdr; break
else
  echo "$ac_t""no" 1>&6
fi
done
# Two versions of opendir et al. are in -ldir and -lx on SCO Xenix.
if test $ac_header_dirent = dirent.h; then
echo $ac_n "checking for -ldir""... $ac_c" 1>&6
ac_lib_var=`echo dir'_'opendir | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-ldir  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3921 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char opendir();

int main() { return 0; }
int t() {
opendir()
; return 0; }
EOF
if { (eval echo configure:3933: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="$LIBS -ldir"
else
  echo "$ac_t""no" 1>&6
fi

else
echo $ac_n "checking for -lx""... $ac_c" 1>&6
ac_lib_var=`echo x'_'opendir | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lx  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 3960 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char opendir();

int main() { return 0; }
int t() {
opendir()
; return 0; }
EOF
if { (eval echo configure:3972: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  LIBS="$LIBS -lx"
else
  echo "$ac_t""no" 1>&6
fi

fi

echo $ac_n "checking for sys/wait.h that is POSIX.1 compatible""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_sys_wait_h'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 3997 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <sys/wait.h>
#ifndef WEXITSTATUS
#define WEXITSTATUS(stat_val) ((unsigned)(stat_val) >> 8)
#endif
#ifndef WIFEXITED
#define WIFEXITED(stat_val) (((stat_val) & 255) == 0)
#endif
int main() { return 0; }
int t() {
int s;
wait (&s);
s = WIFEXITED (s) ? WEXITSTATUS (s) : 1;
; return 0; }
EOF
if { (eval echo configure:4014: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_header_sys_wait_h=yes
else
  rm -rf conftest*
  ac_cv_header_sys_wait_h=no
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_header_sys_wait_h" 1>&6
if test $ac_cv_header_sys_wait_h = yes; then
  cat >> confdefs.h <<\EOF
#define HAVE_SYS_WAIT_H 1
EOF

fi

echo $ac_n "checking whether time.h and sys/time.h may both be included""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_time'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4038 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <sys/time.h>
#include <time.h>
int main() { return 0; }
int t() {
struct tm *tp;
; return 0; }
EOF
if { (eval echo configure:4048: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_header_time=yes
else
  rm -rf conftest*
  ac_cv_header_time=no
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_header_time" 1>&6
if test $ac_cv_header_time = yes; then
  cat >> confdefs.h <<\EOF
#define TIME_WITH_SYS_TIME 1
EOF

fi

echo $ac_n "checking whether struct tm is in sys/time.h or time.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_struct_tm'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4072 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <time.h>
int main() { return 0; }
int t() {
struct tm *tp; tp->tm_sec;
; return 0; }
EOF
if { (eval echo configure:4081: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_struct_tm=time.h
else
  rm -rf conftest*
  ac_cv_struct_tm=sys/time.h
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_struct_tm" 1>&6
if test $ac_cv_struct_tm = sys/time.h; then
  cat >> confdefs.h <<\EOF
#define TM_IN_SYS_TIME 1
EOF

fi

echo $ac_n "checking for tm_zone in struct tm""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_struct_tm_zone'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4105 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <$ac_cv_struct_tm>
int main() { return 0; }
int t() {
struct tm tm; tm.tm_zone;
; return 0; }
EOF
if { (eval echo configure:4114: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_struct_tm_zone=yes
else
  rm -rf conftest*
  ac_cv_struct_tm_zone=no
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_struct_tm_zone" 1>&6
if test "$ac_cv_struct_tm_zone" = yes; then
  cat >> confdefs.h <<\EOF
#define HAVE_TM_ZONE 1
EOF

else
  echo $ac_n "checking for tzname""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_var_tzname'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4137 "configure"
#include "confdefs.h"
#include <time.h>
#ifndef tzname /* For SGI.  */
extern char *tzname[]; /* RS6000 and others reject char **tzname.  */
#endif
int main() { return 0; }
int t() {
atoi(*tzname);
; return 0; }
EOF
if { (eval echo configure:4148: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  ac_cv_var_tzname=yes
else
  rm -rf conftest*
  ac_cv_var_tzname=no
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_var_tzname" 1>&6
  if test $ac_cv_var_tzname = yes; then
    cat >> confdefs.h <<\EOF
#define HAVE_TZNAME 1
EOF

  fi
fi


for ac_hdr in arpa/nameser.h lastlog.h libio.h malloc.h memory.h netdb.h \
 netinet/in.h resolv.h termio.h string.h strings.h syslog.h \
 sys/file.h sys/ioctl.h sys/mount.h sys/quota.h \
 sys/fs/ufs_quota.h sys/socket.h sys/statfs.h \
 sys/statvfs.h sys/time.h sys/termio.h sys/vfs.h \
 sys/termios.h sys/unistd.h unistd.h utmp.h utmpx.h varargs.h
do
ac_safe=`echo "$ac_hdr" | tr './\055' '___'`
echo $ac_n "checking for $ac_hdr""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4182 "configure"
#include "confdefs.h"
#include <$ac_hdr>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:4187: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_hdr=HAVE_`echo $ac_hdr | tr 'abcdefghijklmnopqrstuvwxyz./\055' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ___'`
  cat >> confdefs.h <<EOF
#define $ac_tr_hdr 1
EOF
 
else
  echo "$ac_t""no" 1>&6
fi
done

ac_safe=`echo "sgtty.h" | tr './\055' '___'`
echo $ac_n "checking for sgtty.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4217 "configure"
#include "confdefs.h"
#include <sgtty.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:4222: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  ac_cv_header_sgtty_h="yes"
else
  echo "$ac_t""no" 1>&6
fi

if test "$ac_cv_header_varargs_h" != "yes"; then
    echo "configure: warning: you don't have varargs.h" 1>&2
    echo "configure: warning: lib/util/multcat.c will not compile" 1>&2
    echo "configure: warning: lib/util/multcpy.c will not compile" 1>&2
fi

for ac_func in bcopy dbmcachedump dup2 fcntl ftime getcwd getdtablesize \
 gethostname gettimeofday getwd index initgroups \
 killpg memcpy mkdir mkstemp quotactl regexec res_mkquery \
 setpgrp setpgid setreuid sigchild sigset sigsetmask \
 sigsys socket strchr strdup truncate tzset
do
echo $ac_n "checking for $ac_func""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_$ac_func'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4258 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char $ac_func();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
$ac_func();
#endif

; return 0; }
EOF
if { (eval echo configure:4282: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_$ac_func=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_$ac_func=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'$ac_func`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_func=HAVE_`echo $ac_func | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_func 1
EOF
 
else
  echo "$ac_t""no" 1>&6
fi
done

echo $ac_n "checking for strncat""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_strncat'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4309 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char strncat(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char strncat();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_strncat) || defined (__stub___strncat)
choke me
#else
strncat();
#endif

; return 0; }
EOF
if { (eval echo configure:4333: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_strncat=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_strncat=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'strncat`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE_STRNCAT 1
EOF

else
  echo "$ac_t""no" 1>&6

echo "configure: warning: Your system does not have strncat()" 1>&2
echo "configure: warning: the implement strncat() replacement uses the unsecure strcat()!" 1>&2

fi

echo $ac_n "checking for strncpy""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_strncpy'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4362 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char strncpy(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char strncpy();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_strncpy) || defined (__stub___strncpy)
choke me
#else
strncpy();
#endif

; return 0; }
EOF
if { (eval echo configure:4386: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_strncpy=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_strncpy=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'strncpy`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE_STRNCPY 1
EOF

else
  echo "$ac_t""no" 1>&6

echo "configure: warning: Your system does not have strncpy()" 1>&2
echo "configure: warning: the implement strncpy() replacement uses the unsecure strcpy()!" 1>&2

fi

echo $ac_n "checking for snprintf""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_snprintf'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4415 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char snprintf(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char snprintf();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_snprintf) || defined (__stub___snprintf)
choke me
#else
snprintf();
#endif

; return 0; }
EOF
if { (eval echo configure:4439: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_snprintf=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_snprintf=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'snprintf`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE_SNPRINTF 1
EOF

else
  echo "$ac_t""no" 1>&6

echo $ac_n "checking for vsnprintf""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_vsnprintf'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4463 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char vsnprintf(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char vsnprintf();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_vsnprintf) || defined (__stub___vsnprintf)
choke me
#else
vsnprintf();
#endif

; return 0; }
EOF
if { (eval echo configure:4487: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_vsnprintf=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_vsnprintf=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'vsnprintf`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE_VSNPRINTF 1
EOF

else
  echo "$ac_t""no" 1>&6

echo "configure: warning: Your system does not have snprint() nor vsnprintf()" 1>&2
echo "configure: warning: the implement snprintf() replacement uses the unsecure sprintf()!" 1>&2

fi

fi

echo $ac_n "checking for statvfs""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_statvfs'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4518 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char statvfs(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char statvfs();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_statvfs) || defined (__stub___statvfs)
choke me
#else
statvfs();
#endif

; return 0; }
EOF
if { (eval echo configure:4542: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_statvfs=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_statvfs=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'statvfs`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE_STATFS 1
EOF

else
  echo "$ac_t""no" 1>&6
echo $ac_n "checking for statfs""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_statfs'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4565 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char statfs(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char statfs();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_statfs) || defined (__stub___statfs)
choke me
#else
statfs();
#endif

; return 0; }
EOF
if { (eval echo configure:4589: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_statfs=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_statfs=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'statfs`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE_STATFS 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

fi


echo $ac_n "checking whether utime accepts a null argument""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_utime_null'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  rm -f conftestdata; > conftestdata
# Sequent interprets utime(file, 0) to mean use start of epoch.  Wrong.
if test "$cross_compiling" = yes; then
  ac_cv_func_utime_null=no
else
cat > conftest.$ac_ext <<EOF
#line 4622 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <sys/stat.h>
main() {
struct stat s, t;
exit(!(stat ("conftestdata", &s) == 0 && utime("conftestdata", (long *)0) == 0
&& stat("conftestdata", &t) == 0 && t.st_mtime >= s.st_mtime
&& t.st_mtime - s.st_mtime < 120));
}
EOF
{ (eval echo configure:4633: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_func_utime_null=yes
else
  ac_cv_func_utime_null=no
fi
fi
rm -fr conftest*
rm -f core core.* *.core
fi

echo "$ac_t""$ac_cv_func_utime_null" 1>&6
if test $ac_cv_func_utime_null = yes; then
  cat >> confdefs.h <<\EOF
#define HAVE_UTIME_NULL 1
EOF

fi

echo $ac_n "checking for pid_t""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_type_pid_t'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4657 "configure"
#include "confdefs.h"
#include <sys/types.h>
#if STDC_HEADERS
#include <stdlib.h>
#endif
EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "pid_t" >/dev/null 2>&1; then
  rm -rf conftest*
  ac_cv_type_pid_t=yes
else
  rm -rf conftest*
  ac_cv_type_pid_t=no
fi
rm -f conftest*

fi
echo "$ac_t""$ac_cv_type_pid_t" 1>&6
if test $ac_cv_type_pid_t = no; then
  cat >> confdefs.h <<\EOF
#define pid_t int
EOF

fi

ac_safe=`echo "vfork.h" | tr './\055' '___'`
echo $ac_n "checking for vfork.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4689 "configure"
#include "confdefs.h"
#include <vfork.h>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:4694: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_VFORK_H 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

echo $ac_n "checking for working vfork""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_vfork'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  echo $ac_n "checking for vfork""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_vfork'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4726 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char vfork(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char vfork();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_vfork) || defined (__stub___vfork)
choke me
#else
vfork();
#endif

; return 0; }
EOF
if { (eval echo configure:4750: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_vfork=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_vfork=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'vfork`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  :
else
  echo "$ac_t""no" 1>&6
fi

else
cat > conftest.$ac_ext <<EOF
#line 4769 "configure"
#include "confdefs.h"
/* Thanks to Paul Eggert for this test.  */
#include <stdio.h>
#include <sys/types.h>
#include <sys/stat.h>
#ifdef HAVE_UNISTD_H
#include <unistd.h>
#endif
#ifdef HAVE_VFORK_H
#include <vfork.h>
#endif
/* On some sparc systems, changes by the child to local and incoming
   argument registers are propagated back to the parent.
   The compiler is told about this with #include <vfork.h>,
   but some compilers (e.g. gcc -O) don't grok <vfork.h>.
   Test for this by using a static variable whose address
   is put into a register that is clobbered by the vfork.  */
static
#ifdef __cplusplus
sparc_address_test (int arg)
#else
sparc_address_test (arg) int arg;
#endif
{
  static pid_t child;
  if (!child) {
    child = vfork ();
    if (child < 0)
      perror ("vfork");
    if (!child) {
      arg = getpid();
      write(-1, "", 0);
      _exit (arg);
    }
  }
}
main() {
  pid_t parent = getpid ();
  pid_t child;

  sparc_address_test ();

  child = vfork ();

  if (child == 0) {
    /* Here is another test for sparc vfork register problems.
       This test uses lots of local variables, at least
       as many local variables as main has allocated so far
       including compiler temporaries.  4 locals are enough for
       gcc 1.40.3 on a Solaris 4.1.3 sparc, but we use 8 to be safe.
       A buggy compiler should reuse the register of parent
       for one of the local variables, since it will think that
       parent can't possibly be used any more in this routine.
       Assigning to the local variable will thus munge parent
       in the parent process.  */
    pid_t
      p = getpid(), p1 = getpid(), p2 = getpid(), p3 = getpid(),
      p4 = getpid(), p5 = getpid(), p6 = getpid(), p7 = getpid();
    /* Convince the compiler that p..p7 are live; otherwise, it might
       use the same hardware register for all 8 local variables.  */
    if (p != p1 || p != p2 || p != p3 || p != p4
	|| p != p5 || p != p6 || p != p7)
      _exit(1);

    /* On some systems (e.g. IRIX 3.3),
       vfork doesn't separate parent from child file descriptors.
       If the child closes a descriptor before it execs or exits,
       this munges the parent's descriptor as well.
       Test for this by closing stdout in the child.  */
    _exit(close(fileno(stdout)) != 0);
  } else {
    int status;
    struct stat st;

    while (wait(&status) != child)
      ;
    exit(
	 /* Was there some problem with vforking?  */
	 child < 0

	 /* Did the child fail?  (This shouldn't happen.)  */
	 || status

	 /* Did the vfork/compiler bug occur?  */
	 || parent != getpid()

	 /* Did the file descriptor bug occur?  */
	 || fstat(fileno(stdout), &st) != 0
	 );
  }
}
EOF
{ (eval echo configure:4862: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_func_vfork=yes
else
  ac_cv_func_vfork=no
fi
fi
rm -fr conftest*
fi

echo "$ac_t""$ac_cv_func_vfork" 1>&6
if test $ac_cv_func_vfork = no; then
  cat >> confdefs.h <<\EOF
#define vfork fork
EOF

fi

echo $ac_n "checking for wait3 that fills in rusage""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_wait3'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_func_wait3=no
else
cat > conftest.$ac_ext <<EOF
#line 4888 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <sys/time.h>
#include <sys/resource.h>
#include <stdio.h>
/* HP-UX has wait3 but does not fill in rusage at all.  */
main() {
  struct rusage r;
  int i;
  /* Use a field that we can force nonzero --
     voluntary context switches.
     For systems like NeXT and OSF/1 that don't set it,
     also use the system CPU time.  And page faults (I/O) for Linux.  */
  r.ru_nvcsw = 0;
  r.ru_stime.tv_sec = 0;
  r.ru_stime.tv_usec = 0;
  r.ru_majflt = r.ru_minflt = 0;
  switch (fork()) {
  case 0: /* Child.  */
    sleep(1); /* Give up the CPU.  */
    _exit(0);
  case -1: _exit(0); /* What can we do?  */
  default: /* Parent.  */
    wait3(&i, 0, &r);
    sleep(2); /* Avoid "text file busy" from rm on fast HP-UX machines.  */
    exit(r.ru_nvcsw == 0 && r.ru_majflt == 0 && r.ru_minflt == 0
	 && r.ru_stime.tv_sec == 0 && r.ru_stime.tv_usec == 0);
  }
}
EOF
{ (eval echo configure:4919: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_func_wait3=yes
else
  ac_cv_func_wait3=no
fi
fi
rm -fr conftest*
fi

echo "$ac_t""$ac_cv_func_wait3" 1>&6
if test $ac_cv_func_wait3 = yes; then
  cat >> confdefs.h <<\EOF
#define HAVE_WAIT3 1
EOF

fi


echo $ac_n "checking for off_t""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_type_off_t'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4943 "configure"
#include "confdefs.h"
#include <sys/types.h>
#if STDC_HEADERS
#include <stdlib.h>
#endif
EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "off_t" >/dev/null 2>&1; then
  rm -rf conftest*
  ac_cv_type_off_t=yes
else
  rm -rf conftest*
  ac_cv_type_off_t=no
fi
rm -f conftest*

fi
echo "$ac_t""$ac_cv_type_off_t" 1>&6
if test $ac_cv_type_off_t = no; then
  cat >> confdefs.h <<\EOF
#define off_t long
EOF

fi

echo $ac_n "checking return type of signal handlers""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_type_signal'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 4974 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <signal.h>
#ifdef signal
#undef signal
#endif
#ifdef __cplusplus
extern "C" void (*signal (int, void (*)(int)))(int);
#else
void (*signal ()) ();
#endif

int main() { return 0; }
int t() {
int i;
; return 0; }
EOF
if { (eval echo configure:4992: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_type_signal=void
else
  rm -rf conftest*
  ac_cv_type_signal=int
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_type_signal" 1>&6
cat >> confdefs.h <<EOF
#define RETSIGTYPE $ac_cv_type_signal
EOF


echo $ac_n "checking for sys_siglist declaration in signal.h or unistd.h""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_decl_sys_siglist'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 5014 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <signal.h>
/* NetBSD declares sys_siglist in unistd.h.  */
#ifdef HAVE_UNISTD_H
#include <unistd.h>
#endif
int main() { return 0; }
int t() {
char *msg = *(sys_siglist + 1);
; return 0; }
EOF
if { (eval echo configure:5027: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_decl_sys_siglist=yes
else
  rm -rf conftest*
  ac_cv_decl_sys_siglist=no
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_decl_sys_siglist" 1>&6
if test $ac_cv_decl_sys_siglist = yes; then
  cat >> confdefs.h <<\EOF
#define SYS_SIGLIST_DECLARED 1
EOF

fi


if test $ac_cv_prog_gcc = yes; then
    echo $ac_n "checking whether ${CC-cc} needs -traditional""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_prog_gcc_traditional'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
    ac_pattern="Autoconf.*'x'"
  cat > conftest.$ac_ext <<EOF
#line 5054 "configure"
#include "confdefs.h"
#include <sgtty.h>
Autoconf TIOCGETP
EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "$ac_pattern" >/dev/null 2>&1; then
  rm -rf conftest*
  ac_cv_prog_gcc_traditional=yes
else
  rm -rf conftest*
  ac_cv_prog_gcc_traditional=no
fi
rm -f conftest*


  if test $ac_cv_prog_gcc_traditional = no; then
    cat > conftest.$ac_ext <<EOF
#line 5072 "configure"
#include "confdefs.h"
#include <termio.h>
Autoconf TCGETA
EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "$ac_pattern" >/dev/null 2>&1; then
  rm -rf conftest*
  ac_cv_prog_gcc_traditional=yes
fi
rm -f conftest*

  fi
fi

echo "$ac_t""$ac_cv_prog_gcc_traditional" 1>&6
  if test $ac_cv_prog_gcc_traditional = yes; then
    CC="$CC -traditional"
  fi
fi

echo $ac_n "checking for working const""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_c_const'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 5098 "configure"
#include "confdefs.h"

int main() { return 0; }
int t() {

/* Ultrix mips cc rejects this.  */
typedef int charset[2]; const charset x;
/* SunOS 4.1.1 cc rejects this.  */
char const *const *ccp;
char **p;
/* NEC SVR4.0.2 mips cc rejects this.  */
struct point {int x, y;};
static struct point const zero = {0,0};
/* AIX XL C 1.02.0.0 rejects this.
   It does not let you subtract one const X* pointer from another in an arm
   of an if-expression whose if-part is not a constant expression */
const char *g = "string";
ccp = &g + (g ? g-g : 0);
/* HPUX 7.0 cc rejects these. */
++ccp;
p = (char**) ccp;
ccp = (char const *const *) p;
{ /* SCO 3.2v4 cc rejects this.  */
  char *t;
  char const *s = 0 ? (char *) 0 : (char const *) 0;

  *t++ = 0;
}
{ /* Someone thinks the Sun supposedly-ANSI compiler will reject this.  */
  int x[] = {25, 17};
  const int *foo = &x[0];
  ++foo;
}
{ /* Sun SC1.0 ANSI compiler rejects this -- but not the above. */
  typedef const int *iptr;
  iptr p = 0;
  ++p;
}
{ /* AIX XL C 1.02.0.0 rejects this saying
     "k.c", line 2.27: 1506-025 (S) Operand must be a modifiable lvalue. */
  struct s { int j; const int *ap[3]; };
  struct s *b; b->j = 5;
}
{ /* ULTRIX-32 V3.1 (Rev 9) vcc rejects this */
  const int foo = 10;
}

; return 0; }
EOF
if { (eval echo configure:5148: \"$ac_compile\") 1>&5; (eval $ac_compile) 2>&5; }; then
  rm -rf conftest*
  ac_cv_c_const=yes
else
  rm -rf conftest*
  ac_cv_c_const=no
fi
rm -f conftest*

fi

echo "$ac_t""$ac_cv_c_const" 1>&6
if test $ac_cv_c_const = no; then
  cat >> confdefs.h <<\EOF
#define const 
EOF

fi


echo $ac_n "checking whether using nameservercode""... $ac_c" 1>&6
if test "$enable_nameserver" != no; then
  if test "$ac_cv_func_res_mkquery" = "yes"; then
    echo "$ac_t""yes" 1>&6
  else
    echo $ac_n "checking for -lresolv""... $ac_c" 1>&6
ac_lib_var=`echo resolv'_'dn_expand | tr './+\055' '__p_'`
if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_save_LIBS="$LIBS"
LIBS="-lresolv  $LIBS"
cat > conftest.$ac_ext <<EOF
#line 5181 "configure"
#include "confdefs.h"
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char dn_expand();

int main() { return 0; }
int t() {
dn_expand()
; return 0; }
EOF
if { (eval echo configure:5193: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=yes"
else
  rm -rf conftest*
  eval "ac_cv_lib_$ac_lib_var=no"
fi
rm -f conftest*
LIBS="$ac_save_LIBS"

fi
if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_lib=HAVE_LIB`echo resolv | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
  cat >> confdefs.h <<EOF
#define $ac_tr_lib 1
EOF

  LIBS="-lresolv $LIBS"

else
  echo "$ac_t""no" 1>&6
fi

  fi
  echo $ac_n "checking for _getshort""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func__getshort'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 5223 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char _getshort(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char _getshort();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub__getshort) || defined (__stub____getshort)
choke me
#else
_getshort();
#endif

; return 0; }
EOF
if { (eval echo configure:5247: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func__getshort=yes"
else
  rm -rf conftest*
  eval "ac_cv_func__getshort=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'_getshort`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE__GETSHORT 1
EOF

else
  echo "$ac_t""no" 1>&6

  echo $ac_n "checking for getshort""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_func_getshort'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 5271 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char getshort(); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
/* We use char because int might match the return type of a gcc2
    builtin and then its argument prototype would still apply.  */
char getshort();

int main() { return 0; }
int t() {

/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_getshort) || defined (__stub___getshort)
choke me
#else
getshort();
#endif

; return 0; }
EOF
if { (eval echo configure:5295: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }; then
  rm -rf conftest*
  eval "ac_cv_func_getshort=yes"
else
  rm -rf conftest*
  eval "ac_cv_func_getshort=no"
fi
rm -f conftest*

fi
if eval "test \"`echo '$ac_cv_func_'getshort`\" = yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<EOF
#define HAVE_GETSHORT 1
EOF

else
  echo "$ac_t""no" 1>&6
echo "configure: warning: Cannot find _getshort() nor getshort()" 1>&2
  
fi

fi

else
    echo "configure: warning: no" 1>&2
fi

echo $ac_n "checking whether sprintf returns number of printed chars""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_returns_n_of_chars'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_returns_n_of_chars=no
else
cat > conftest.$ac_ext <<EOF
#line 5331 "configure"
#include "confdefs.h"
#include <stdio.h> 
 main() { char *str=(char*)malloc(20);
 if (((int) sprintf(str,"123456789")) == 9) exit(0); else exit(1); }
EOF
{ (eval echo configure:5337: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_returns_n_of_chars=yes
else
  ac_cv_returns_n_of_chars=no
fi
fi
rm -fr conftest*
fi

if test "$ac_cv_returns_n_of_chars" = yes; then
  echo "$ac_t""yes" 1>&6
else
  echo "$ac_t""no" 1>&6
  cat >> confdefs.h <<\EOF
#define BSD_SPRINTF 1
EOF

fi

echo $ac_n "checking whether have SIGSYS""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_have_SIGSYS'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_SIGSYS=no
else
cat > conftest.$ac_ext <<EOF
#line 5365 "configure"
#include "confdefs.h"
#include <signal.h> 
 main() { if(SIGSYS>0) exit(0); else exit(1);}
EOF
{ (eval echo configure:5370: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_SIGSYS=yes
else
  ac_cv_have_SIGSYS=no
fi
fi
rm -fr conftest*
fi

if test "$ac_cv_have_SIGSYS" = yes; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_DEF_SIGSYS 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

echo $ac_n "checking whether sys_errlist is declared""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_have_SYS_ERRLIST'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_SYS_ERRLIST=no
else
cat > conftest.$ac_ext <<EOF
#line 5398 "configure"
#include "confdefs.h"
#include <stdio.h>
 main() { extern int sys_errlist; exit(0); }
EOF
{ (eval echo configure:5403: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_SYS_ERRLIST=yes
else
  ac_cv_have_SYS_ERRLIST=no
fi
fi
rm -fr conftest*
fi

if test "$ac_cv_have_SYS_ERRLIST" = "yes"; then
  echo "$ac_t""no" 1>&6
else
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_SYS_ERRLIST_DECL 1
EOF

fi

echo $ac_n "checking whether have struct __res_state""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_have_STATE'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_STATE=no
else
cat > conftest.$ac_ext <<EOF
#line 5431 "configure"
#include "confdefs.h"

#ifdef HAVE_NETINET_IN_H
#  include <netinet/in.h>
#endif
#ifdef HAVE_NETDB_H
#  include <netdb.h>
#endif
#ifdef HAVE_SYS_SOCKET_H
#  include <sys/socket.h>
#endif
#ifdef HAVE_ARPA_NAMESER_H
#  include <arpa/nameser.h>
#endif
#ifdef HAVE_RESOLV_H
#  include <resolv.h> 
#endif
 main() { struct __res_state res; exit(0); }
EOF
{ (eval echo configure:5451: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_STATE=yes
else
  ac_cv_have_STATE=no
fi
fi
rm -fr conftest*
  if test "$ac_cv_have_STATE" != "yes"; then
    cat > conftest.$ac_ext <<EOF
#line 5461 "configure"
#include "confdefs.h"
#include <resolv.h>
EOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "__res_state" >/dev/null 2>&1; then
  rm -rf conftest*
  ac_cv_have_STATE=yes
fi
rm -f conftest*

  fi
fi

if test "$ac_cv_have_STATE" = "yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_RES_STATE 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

echo $ac_n "checking whether have _NFILE""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_have_NFILE'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_NFILE=no
else
cat > conftest.$ac_ext <<EOF
#line 5493 "configure"
#include "confdefs.h"
#include <stdio.h>
 main() { 
#ifdef _NFILE
exit(0);
#else
exit(1);
#endif
 }
EOF
{ (eval echo configure:5504: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_NFILE=yes
else
  ac_cv_have_NFILE=no
fi
fi
rm -fr conftest*
fi

if test "$ac_cv_have_NFILE" = yes; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_NFILE 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

if test "$ac_cv_func_mkdir" = "yes"; then
  echo $ac_n "checking what argument mkdir() takes a second argument""... $ac_c" 1>&6
  if eval "test \"`echo '$''{'ac_cv_have_MKDIR_LNX'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_MKDIR_LNX=no
else
cat > conftest.$ac_ext <<EOF
#line 5533 "configure"
#include "confdefs.h"
#include <sys/stat.h>
     main() { 
	mkdir("dummy", 0577);
	exit(0);
   }
EOF
{ (eval echo configure:5541: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_MKDIR_LNX=yes
else
  ac_cv_have_MKDIR_LNX=no
fi
fi
rm -fr conftest*
fi

  if test "$ac_cv_have_MKDIR_LNX" = yes; then
    echo "$ac_t""yes" 1>&6
    cat >> confdefs.h <<\EOF
#define MKDIR_HAVE_SECOND_ARG 1
EOF

  else
    echo "$ac_t""no" 1>&6
  fi
  if test -x dummy; then
    rmdir dummy
  fi
fi

if test "$ac_cv_header_fcntl_h" = "yes"; then
  echo $ac_n "checking whether fcntl(2, F_DUPFD, fd) works""... $ac_c" 1>&6
  if eval "test \"`echo '$''{'ac_cv_have_FCNTL_F_DUPFD'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_FCNTL_F_DUPFD=no
else
cat > conftest.$ac_ext <<EOF
#line 5574 "configure"
#include "confdefs.h"
#include <fcntl.h>
     main() { 
	int fd, fd2;
  	fd2 = fcntl(2, F_DUPFD, fd);
	if(fd2>0) exit(0); else exit(1);
   }
EOF
{ (eval echo configure:5583: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_FCNTL_F_DUPFD=yes
else
  ac_cv_have_FCNTL_F_DUPFD=no
fi
fi
rm -fr conftest*
fi

  if test "$ac_cv_have_FCNTL_F_DUPFD" = yes; then
    echo "$ac_t""yes" 1>&6
    cat >> confdefs.h <<\EOF
#define HAVE_FCNTL_F_DUPFD 1
EOF

  else
    echo "$ac_t""no" 1>&6
  fi
fi

if test "$ac_cv_header_sys_ioctl_h" = "yes"; then
  echo $ac_n "checking whether ioctl(1, TIOCSBRK, 0) works""... $ac_c" 1>&6
  if eval "test \"`echo '$''{'ac_cv_have_TIOCSBRK'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_TIOCSBRK=no
else
cat > conftest.$ac_ext <<EOF
#line 5613 "configure"
#include "confdefs.h"
#include <sys/ioctl.h>
     main() { 
	ioctl(1, TIOCSBRK, 0)
	exit(0);
   }
EOF
{ (eval echo configure:5621: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_TIOCSBRK=yes
else
  ac_cv_have_TIOCSBRK=no
fi
fi
rm -fr conftest*
fi

  if test "$ac_cv_have_TIOCSBRK" = yes; then
    echo "$ac_t""yes" 1>&6
    cat >> confdefs.h <<\EOF
#define HAVE_IOCTL_TIOCSBRK 1
EOF

  else
    echo "$ac_t""no" 1>&6
  fi
  ac_cv_header_sys_ioctl_h=$ac_cv_have_TIOCSBRK
fi

if test "$ac_cv_header_sgtty_h" = "yes"; then
  echo $ac_n "checking whether CBREAK is defined in <sgtty.h>""... $ac_c" 1>&6
  if eval "test \"`echo '$''{'ac_cv_have_CBREAK'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_CBREAK=no
else
cat > conftest.$ac_ext <<EOF
#line 5652 "configure"
#include "confdefs.h"
#include <sgtty.h>
     main() { 
#ifdef CBREAK
	exit(0);
#else
	exit(1);
#endif
   }
EOF
{ (eval echo configure:5663: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_CBREAK=yes
else
  ac_cv_have_CBREAK=no
fi
fi
rm -fr conftest*
fi

  if test "$ac_cv_have_CBREAK" = yes; then
    echo "$ac_t""yes" 1>&6
    cat >> confdefs.h <<\EOF
#define HAVE_SGTTY_CBREAK 1
EOF

    cat >> confdefs.h <<\EOF
#define HAVE_SGTTY_H 1
EOF

  else
    echo "$ac_t""no" 1>&6
  fi
  echo $ac_n "checking whether TIOCSETN is defined in <sgtty.h>""... $ac_c" 1>&6
  if eval "test \"`echo '$''{'ac_cv_have_TIOCSETN'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_TIOCSETN=no
else
cat > conftest.$ac_ext <<EOF
#line 5694 "configure"
#include "confdefs.h"
#include <sgtty.h>
     main() { 
#ifdef TIOCSETN
	exit(0);
#else
	exit(1);
#endif
   }
EOF
{ (eval echo configure:5705: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_TIOCSETN=yes
else
  ac_cv_have_TIOCSETN=no
fi
fi
rm -fr conftest*
fi

  if test "$ac_cv_have_TIOCSETN" = yes; then
    echo "$ac_t""yes" 1>&6
    cat >> confdefs.h <<\EOF
#define HAVE_SGTTY_TIOCSETN 1
EOF

    cat >> confdefs.h <<\EOF
#define HAVE_SGTTY_H 1
EOF

  else
    echo "$ac_t""no" 1>&6
    for ac_hdr in sys/strtio.h
do
ac_safe=`echo "$ac_hdr" | tr './\055' '___'`
echo $ac_n "checking for $ac_hdr""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_header_$ac_safe'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  cat > conftest.$ac_ext <<EOF
#line 5735 "configure"
#include "confdefs.h"
#include <$ac_hdr>
EOF
ac_try="$ac_cpp conftest.$ac_ext >/dev/null 2>conftest.out"
{ (eval echo configure:5740: \"$ac_try\") 1>&5; (eval $ac_try) 2>&5; }
ac_err=`grep -v '^ *+' conftest.out`
if test -z "$ac_err"; then
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=yes"
else
  echo "$ac_err" >&5
  rm -rf conftest*
  eval "ac_cv_header_$ac_safe=no"
fi
rm -f conftest*
fi
if eval "test \"`echo '$ac_cv_header_'$ac_safe`\" = yes"; then
  echo "$ac_t""yes" 1>&6
    ac_tr_hdr=HAVE_`echo $ac_hdr | tr 'abcdefghijklmnopqrstuvwxyz./\055' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ___'`
  cat >> confdefs.h <<EOF
#define $ac_tr_hdr 1
EOF
 
else
  echo "$ac_t""no" 1>&6
fi
done

    if test "$ac_cv_header_sys_strtio_h" = "yes"; then
      echo $ac_n "checking whether TIOCSETN is defined in <sys/strtio.h>""... $ac_c" 1>&6
      if test "$cross_compiling" = yes; then
  ac_cv_have_TIOCSETN=no
else
cat > conftest.$ac_ext <<EOF
#line 5770 "configure"
#include "confdefs.h"
#include <sgtty.h>
#include <sys/strtio.h>
       main() { 
#ifdef TIOCSETN
	  exit(0);
#else
	  exit(1);
#endif
       }
EOF
{ (eval echo configure:5782: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_TIOCSETN=yes
else
  ac_cv_have_TIOCSETN=no
fi
fi
rm -fr conftest*
      if test "$ac_cv_have_TIOCSETN" = yes; then
        echo "$ac_t""yes" 1>&6
        cat >> confdefs.h <<\EOF
#define HAVE_SGTTY_TIOCSETN 1
EOF

       cat >> confdefs.h <<\EOF
#define HAVE_SGTTY_H 1
EOF

      else
        echo "$ac_t""no" 1>&6
      fi
    else
      if test "$ac_cv_header_termio_h" = "yes"; then
        echo $ac_n "checking whether TIOCSETN is defined in <termio.h>""... $ac_c" 1>&6
        if test "$cross_compiling" = yes; then
  ac_cv_have_TERMIO_TIOCSETN=no
else
cat > conftest.$ac_ext <<EOF
#line 5810 "configure"
#include "confdefs.h"
#include <termio.h>
          main() { 
#ifdef TIOCSETN
    		exit(0);
#else
		exit(1);
#endif
        }
EOF
{ (eval echo configure:5821: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_TERMIO_TIOCSETN=yes
else
  ac_cv_have_TERMIO_TIOCSETN=no
fi
fi
rm -fr conftest*
        if test "$ac_cv_have_TERMIO_TIOCSETN" = yes; then
          echo "$ac_t""yes" 1>&6
          ac_cv_header_sgtty_h="no"
          cat >> confdefs.h <<\EOF
#define HAVE_TERMIO_TIOCSETN 1
EOF

        else
          echo "$ac_t""no" 1>&6
	fi
      fi
    fi
  fi
fi

if test "$ac_cv_header_varargs_h" != "yes"; then
  echo $ac_n "checking if compiler supports variable number of arguments""... $ac_c" 1>&6
  if eval "test \"`echo '$''{'ac_cv_VAR_NBR_ARGS'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_VAR_NBR_ARGS=no
else
cat > conftest.$ac_ext <<EOF
#line 5853 "configure"
#include "confdefs.h"
 int var_t1(a)
	char *a;
	{
	  return(var_t2(&a));
	}

	int var_t2(argv)
	char **argv;
	{
	  if((strcmp(argv0,argv1)!=0)&&(strcmp(argv0,argv2)==0))
		return(0);
	  else return(1);
	}
	main()
	{
 	 exit(var_t1("abs", "qwe", "abs"));
   }
EOF
{ (eval echo configure:5873: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_VAR_NBR_ARGS=yes
else
  ac_cv_VAR_NBR_ARGS=no
fi
fi
rm -fr conftest*
fi

  if test "$ac_cv_VAR_NBR_ARGS" = "yes"; then
    echo "$ac_t""yes" 1>&6
  else
    echo "$ac_t""no" 1>&6
    cat >> confdefs.h <<\EOF
#define NO_VARARGS 1
EOF

  fi
fi

echo $ac_n "checking whether have union wait""... $ac_c" 1>&6
if eval "test \"`echo '$''{'ac_cv_have_UNION_WAIT'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  if test "$cross_compiling" = yes; then
  ac_cv_have_UNION_WAIT=no
else
cat > conftest.$ac_ext <<EOF
#line 5902 "configure"
#include "confdefs.h"
#include <sys/wait.h>
 main() { 
	union wait pstatus;
	exit(0); }
EOF
{ (eval echo configure:5909: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; }
if test -s conftest && (./conftest; exit) 2>/dev/null; then
  ac_cv_have_UNION_WAIT=yes
else
  ac_cv_have_UNION_WAIT=no
fi
fi
rm -fr conftest*
fi

if test "$ac_cv_have_UNION_WAIT" = "yes"; then
  echo "$ac_t""yes" 1>&6
  cat >> confdefs.h <<\EOF
#define HAVE_UNION_WAIT 1
EOF

else
  echo "$ac_t""no" 1>&6
fi

if test "$ac_cv_enable_ucbmail" = "yes" -o \
	"$enable_ucbmail" = "yes"; then
  if test "$XSTR" = ""; then
    echo "configure: warning: Disable ucbMail. xstr not found. Cannot compile ucbMail" 1>&2
    enable_ucbmail="no"
    ac_cv_enable_ucbmail="no"
  fi
fi

echo

  echo $ac_n "checking whether define DEBUG""... $ac_c" 1>&6
  if test "$mmdfdebug" != "0"; then
    cat >> confdefs.h <<EOF
#define DEBUG $mmdfdebug
EOF

    echo "$ac_t""yes" 1>&6
  else
        echo "$ac_t""no" 1>&6
  fi


  echo $ac_n "checking whether define D_LOG""... $ac_c" 1>&6
  if test "$mmdfdlog" != "0"; then
    cat >> confdefs.h <<EOF
#define D_LOG $mmdfdlog
EOF

    echo "$ac_t""yes" 1>&6
  else
        echo "$ac_t""no" 1>&6
  fi


  echo $ac_n "checking whether define D_DBGLOG""... $ac_c" 1>&6
  if test "$mmdfdbglog" != "0"; then
    cat >> confdefs.h <<EOF
#define D_DBGLOG $mmdfdbglog
EOF

    echo "$ac_t""yes" 1>&6
  else
        echo "$ac_t""no" 1>&6
  fi


  echo $ac_n "checking whether define NODOMLIT""... $ac_c" 1>&6
  if test "$mmdfnodomlit" != "0"; then
    cat >> confdefs.h <<EOF
#define NODOMLIT $mmdfnodomlit
EOF

    echo "$ac_t""yes" 1>&6
  else
        echo "$ac_t""no" 1>&6
  fi


  echo $ac_n "checking whether define LEFTDOTS""... $ac_c" 1>&6
  if test "$mmdfleftdots" != "0"; then
    cat >> confdefs.h <<EOF
#define LEFTDOTS $mmdfleftdots
EOF

    echo "$ac_t""yes" 1>&6
  else
        echo "$ac_t""no" 1>&6
  fi


  echo $ac_n "checking whether define STATSORT""... $ac_c" 1>&6
  if test "$mmdfstatsort" != "0"; then
    cat >> confdefs.h <<EOF
#define STATSORT $mmdfstatsort
EOF

    echo "$ac_t""yes" 1>&6
  else
        echo "$ac_t""no" 1>&6
  fi


  echo $ac_n "checking whether define CITATION""... $ac_c" 1>&6
  if test "$mmdfcitation" != "0"; then
    cat >> confdefs.h <<EOF
#define CITATION $mmdfcitation
EOF

    echo "$ac_t""yes" 1>&6
  else
        echo "$ac_t""no" 1>&6
  fi



  echo $ac_n "checking whether using "tcp wrapper"""... $ac_c" 1>&6
  if test "$enable_wrap" != "yes" && test "$enable_wrap" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_lib_wrap'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_lib_wrap="yes"
fi

    enable_wrap=$ac_cv_enable_lib_wrap
  else
    ac_cv_enable_lib_wrap=$enable_wrap
  fi
  if test "$enable_wrap" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "lib" != "lib"; then
      var=`echo wrap | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_lib_wrap" != "yes"; then
        echo "configure: warning: "cannot enable "tcp wrapper": library libwrap not found. disabling"" 1>&2;
        enable_wrap="no"
        ac_cv_enable_lib_wrap=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "nameserver"""... $ac_c" 1>&6
  if test "$enable_nameserver" != "yes" && test "$enable_nameserver" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_nameserver'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_nameserver="yes"
fi

    enable_nameserver=$ac_cv_enable_code_nameserver
  else
    ac_cv_enable_code_nameserver=$enable_nameserver
  fi
  if test "$enable_nameserver" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo nameserver | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_nameserver" != "yes"; then
        echo "configure: warning: "cannot enable "nameserver": library codenameserver not found. disabling"" 1>&2;
        enable_nameserver="no"
        ac_cv_enable_code_nameserver=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "dbm"""... $ac_c" 1>&6
  if test "$enable_dbm" != "yes" && test "$enable_dbm" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_lib_dbm'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_lib_dbm="yes"
fi

    enable_dbm=$ac_cv_enable_lib_dbm
  else
    ac_cv_enable_lib_dbm=$enable_dbm
  fi
  if test "$enable_dbm" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "lib" != "lib"; then
      var=`echo dbm | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_lib_dbm" != "yes"; then
        echo "configure: warning: "cannot enable "dbm": library libdbm not found. disabling"" 1>&2;
        enable_dbm="no"
        ac_cv_enable_lib_dbm=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "tables can come via NIS"""... $ac_c" 1>&6
  if test "$enable_nis" != "yes" && test "$enable_nis" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_nis'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_nis="no "
fi

    enable_nis=$ac_cv_enable_code_nis
  else
    ac_cv_enable_code_nis=$enable_nis
  fi
  if test "$enable_nis" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo nis | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_nis" != "yes"; then
        echo "configure: warning: "cannot enable "tables can come via NIS": library codenis not found. disabling"" 1>&2;
        enable_nis="no"
        ac_cv_enable_code_nis=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "'mailids' and 'users' configurable in mmdftailor"""... $ac_c" 1>&6
  if test "$enable_nn" != "yes" && test "$enable_nn" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_nn'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_nn="no "
fi

    enable_nn=$ac_cv_enable_code_nn
  else
    ac_cv_enable_code_nn=$enable_nn
  fi
  if test "$enable_nn" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo nn | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_nn" != "yes"; then
        echo "configure: warning: "cannot enable "'mailids' and 'users' configurable in mmdftailor": library codenn not found. disabling"" 1>&2;
        enable_nn="no"
        ac_cv_enable_code_nn=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "Source routing"""... $ac_c" 1>&6
  if test "$enable_nosrcroute" != "yes" && test "$enable_nosrcroute" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_nosrcroute'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_nosrcroute="yes"
fi

    enable_nosrcroute=$ac_cv_enable_code_nosrcroute
  else
    ac_cv_enable_code_nosrcroute=$enable_nosrcroute
  fi
  if test "$enable_nosrcroute" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo nosrcroute | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_nosrcroute" != "yes"; then
        echo "configure: warning: "cannot enable "Source routing": library codenosrcroute not found. disabling"" 1>&2;
        enable_nosrcroute="no"
        ac_cv_enable_code_nosrcroute=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "8bit-clean transfer"""... $ac_c" 1>&6
  if test "$enable_8bit" != "yes" && test "$enable_8bit" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_8bit'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_8bit="yes"
fi

    enable_8bit=$ac_cv_enable_code_8bit
  else
    ac_cv_enable_code_8bit=$enable_8bit
  fi
  if test "$enable_8bit" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo 8bit | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_8bit" != "yes"; then
        echo "configure: warning: "cannot enable "8bit-clean transfer": library code8bit not found. disabling"" 1>&2;
        enable_8bit="no"
        ac_cv_enable_code_8bit=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "ESMTP-support"""... $ac_c" 1>&6
  if test "$enable_esmtp" != "yes" && test "$enable_esmtp" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_esmtp'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_esmtp="no"
fi

    enable_esmtp=$ac_cv_enable_code_esmtp
  else
    ac_cv_enable_code_esmtp=$enable_esmtp
  fi
  if test "$enable_esmtp" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo esmtp | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_esmtp" != "yes"; then
        echo "configure: warning: "cannot enable "ESMTP-support": library codeesmtp not found. disabling"" 1>&2;
        enable_esmtp="no"
        ac_cv_enable_code_esmtp=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "ESMTP-DSN-support"""... $ac_c" 1>&6
  if test "$enable_esmtp_dsn" != "yes" && test "$enable_esmtp_dsn" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_esmtp_dsn'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_esmtp_dsn="no"
fi

    enable_esmtp_dsn=$ac_cv_enable_code_esmtp_dsn
  else
    ac_cv_enable_code_esmtp_dsn=$enable_esmtp_dsn
  fi
  if test "$enable_esmtp_dsn" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo esmtp_dsn | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_esmtp_dsn" != "yes"; then
        echo "configure: warning: "cannot enable "ESMTP-DSN-support": library codeesmtp_dsn not found. disabling"" 1>&2;
        enable_esmtp_dsn="no"
        ac_cv_enable_code_esmtp_dsn=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "ESMTP-8BITMIME-support"""... $ac_c" 1>&6
  if test "$enable_esmtp_8bitmime" != "yes" && test "$enable_esmtp_8bitmime" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_esmtp_8bitmime'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_esmtp_8bitmime="no"
fi

    enable_esmtp_8bitmime=$ac_cv_enable_code_esmtp_8bitmime
  else
    ac_cv_enable_code_esmtp_8bitmime=$enable_esmtp_8bitmime
  fi
  if test "$enable_esmtp_8bitmime" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo esmtp_8bitmime | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_esmtp_8bitmime" != "yes"; then
        echo "configure: warning: "cannot enable "ESMTP-8BITMIME-support": library codeesmtp_8bitmime not found. disabling"" 1>&2;
        enable_esmtp_8bitmime="no"
        ac_cv_enable_code_esmtp_8bitmime=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "Wildcard-support"""... $ac_c" 1>&6
  if test "$enable_wildcard" != "yes" && test "$enable_wildcard" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_wildcard'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_wildcard="no"
fi

    enable_wildcard=$ac_cv_enable_code_wildcard
  else
    ac_cv_enable_code_wildcard=$enable_wildcard
  fi
  if test "$enable_wildcard" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo wildcard | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_wildcard" != "yes"; then
        echo "configure: warning: "cannot enable "Wildcard-support": library codewildcard not found. disabling"" 1>&2;
        enable_wildcard="no"
        ac_cv_enable_code_wildcard=no
      fi
    fi
  fi


  echo $ac_n "checking whether using "rbl-support"""... $ac_c" 1>&6
  if test "$enable_rbl" != "yes" && test "$enable_rbl" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_code_rbl'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_code_rbl="no"
fi

    enable_rbl=$ac_cv_enable_code_rbl
  else
    ac_cv_enable_code_rbl=$enable_rbl
  fi
  if test "$enable_rbl" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    if test "code" != "lib"; then
      var=`echo rbl | tr 'abcdefghijklmnopqrstuvwxyz' 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'`
      cat >> confdefs.h <<EOF
#define HAVE_$var 1
EOF

    else
      if test "$ac_cv_enable_code_rbl" != "yes"; then
        echo "configure: warning: "cannot enable "rbl-support": library coderbl not found. disabling"" 1>&2;
        enable_rbl="no"
        ac_cv_enable_code_rbl=no
      fi
    fi
  fi


if test "$ac_cv_enable_lib_dbm" = "yes"; then
   CH_TB="ch_tbdbm"
else
   CH_TB="ch_tbseq"
fi
if test "$enable_8bit" = "yes"; then
   cat >> confdefs.h <<\EOF
#define HAVE_8BIT 1
EOF

fi


echo
if test -d lib; then
  SUBDIRS="$SUBDIRS lib"
fi
if test -d src; then
  SUBDIRS="$SUBDIRS src"
fi
if test -d uip; then
  SUBDIRS="$SUBDIRS uip"
fi
if test -d man; then
  SUBDIRS="$SUBDIRS man"
fi

LIBSUBDIRS="addr mmdf table util"

  echo $ac_n "checking whether compiling dial""... $ac_c" 1>&6
  if test "$enable_dial" != "yes" && test "$enable_dial" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_dial'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_dial="no"
fi

    enable_dial=$ac_cv_enable_dial
  else
    ac_cv_enable_dial=$enable_dial
  fi
  
  if test "$enable_dial" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    LIBSUBDIRS="$LIBSUBDIRS dial"
  fi


SRCSUBDIRS="deliver local submit tools"

  echo $ac_n "checking whether compiling badusers""... $ac_c" 1>&6
  if test "$enable_badusers" != "yes" && test "$enable_badusers" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_badusers'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_badusers="yes"
fi

    enable_badusers=$ac_cv_enable_badusers
  else
    ac_cv_enable_badusers=$enable_badusers
  fi
  
  if test "$enable_badusers" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS badusers"
  fi


  echo $ac_n "checking whether compiling bboards""... $ac_c" 1>&6
  if test "$enable_bboards" != "yes" && test "$enable_bboards" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_bboards'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_bboards="no"
fi

    enable_bboards=$ac_cv_enable_bboards
  else
    ac_cv_enable_bboards=$enable_bboards
  fi
  
  if test "$enable_bboards" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS bboards"
  fi


  echo $ac_n "checking whether compiling blockaddr""... $ac_c" 1>&6
  if test "$enable_blockaddr" != "yes" && test "$enable_blockaddr" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_blockaddr'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_blockaddr="yes"
fi

    enable_blockaddr=$ac_cv_enable_blockaddr
  else
    ac_cv_enable_blockaddr=$enable_blockaddr
  fi
  
  if test "$enable_blockaddr" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS blockaddr"
  fi


  echo $ac_n "checking whether compiling delay""... $ac_c" 1>&6
  if test "$enable_delay" != "yes" && test "$enable_delay" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_delay'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_delay="yes"
fi

    enable_delay=$ac_cv_enable_delay
  else
    ac_cv_enable_delay=$enable_delay
  fi
  
  if test "$enable_delay" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS delay"
  fi


  echo $ac_n "checking whether compiling ean""... $ac_c" 1>&6
  if test "$enable_ean" != "yes" && test "$enable_ean" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_ean'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_ean="no"
fi

    enable_ean=$ac_cv_enable_ean
  else
    ac_cv_enable_ean=$enable_ean
  fi
  
  if test "$enable_ean" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS ean"
  fi


  echo $ac_n "checking whether compiling list""... $ac_c" 1>&6
  if test "$enable_list" != "yes" && test "$enable_list" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_list'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_list="yes"
fi

    enable_list=$ac_cv_enable_list
  else
    ac_cv_enable_list=$enable_list
  fi
  
  if test "$enable_list" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS list"
  fi


  echo $ac_n "checking whether compiling niftp""... $ac_c" 1>&6
  if test "$enable_niftp" != "yes" && test "$enable_niftp" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_niftp'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_niftp="no"
fi

    enable_niftp=$ac_cv_enable_niftp
  else
    ac_cv_enable_niftp=$enable_niftp
  fi
  
  if test "$enable_niftp" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS niftp"
  fi


  echo $ac_n "checking whether compiling phone""... $ac_c" 1>&6
  if test "$enable_phone" != "yes" && test "$enable_phone" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_phone'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_phone="no "
fi

    enable_phone=$ac_cv_enable_phone
  else
    ac_cv_enable_phone=$enable_phone
  fi
  
  if test "$enable_phone" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS phone"
  fi


  echo $ac_n "checking whether compiling pobox""... $ac_c" 1>&6
  if test "$enable_pobox" != "yes" && test "$enable_pobox" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_pobox'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_pobox="yes"
fi

    enable_pobox=$ac_cv_enable_pobox
  else
    ac_cv_enable_pobox=$enable_pobox
  fi
  
  if test "$enable_pobox" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS pobox"
  fi


  echo $ac_n "checking whether compiling pop""... $ac_c" 1>&6
  if test "$enable_pop" != "yes" && test "$enable_pop" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_pop'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_pop="no "
fi

    enable_pop=$ac_cv_enable_pop
  else
    ac_cv_enable_pop=$enable_pop
  fi
  
  if test "$enable_pop" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS pop"
  fi


  echo $ac_n "checking whether compiling prog""... $ac_c" 1>&6
  if test "$enable_prog" != "yes" && test "$enable_prog" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_prog'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_prog="yes"
fi

    enable_prog=$ac_cv_enable_prog
  else
    ac_cv_enable_prog=$enable_prog
  fi
  
  if test "$enable_prog" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS prog"
  fi


  echo $ac_n "checking whether compiling smphone""... $ac_c" 1>&6
  if test "$enable_smphone" != "yes" && test "$enable_smphone" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_smphone'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_smphone="no "
fi

    enable_smphone=$ac_cv_enable_smphone
  else
    ac_cv_enable_smphone=$enable_smphone
  fi
  
  if test "$enable_smphone" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS smphone"
  fi


  echo $ac_n "checking whether compiling smtp""... $ac_c" 1>&6
  if test "$enable_smtp" != "yes" && test "$enable_smtp" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_smtp'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_smtp="yes"
fi

    enable_smtp=$ac_cv_enable_smtp
  else
    ac_cv_enable_smtp=$enable_smtp
  fi
  
  if test "$enable_smtp" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS smtp"
  fi


  echo $ac_n "checking whether compiling uucp""... $ac_c" 1>&6
  if test "$enable_uucp" != "yes" && test "$enable_uucp" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_uucp'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_uucp="yes"
fi

    enable_uucp=$ac_cv_enable_uucp
  else
    ac_cv_enable_uucp=$enable_uucp
  fi
  
  if test "$enable_uucp" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    SRCSUBDIRS="$SRCSUBDIRS uucp"
  fi


if test "$enable_phone" = "yes" && test "$enable_dial" = "no"; then
    echo "configure: warning: need lib/dial for phone-channel...enabled" 1>&2
    enable_dial="yes"
    ac_cv_enable_dial="yes"
    LIBSUBDIRS=$LIBSUBDIRS" dial"
fi
if test "$enable_smphone" = "yes" && test "$enable_dial" = "no"; then
    echo "configure: warning: need lib/dial for smphone-channel...enabled" 1>&2
    enable_dial="yes"
    ac_cv_enable_dial="yes"
    LIBSUBDIRS=$LIBSUBDIRS" dial"
fi
if test "$enable_dial" = "yes"; then
  cat >> confdefs.h <<\EOF
#define HAVE_DIAL 1
EOF

fi


UIPSUBDIRS=""

  echo $ac_n "checking whether compiling msg""... $ac_c" 1>&6
  if test "$enable_msg" != "yes" && test "$enable_msg" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_msg'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_msg="yes"
fi

    enable_msg=$ac_cv_enable_msg
  else
    ac_cv_enable_msg=$enable_msg
  fi
  
  if test "$enable_msg" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    UIPSUBDIRS="$UIPSUBDIRS msg"
  fi


  echo $ac_n "checking whether compiling other""... $ac_c" 1>&6
  if test "$enable_other" != "yes" && test "$enable_other" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_other'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_other="yes"
fi

    enable_other=$ac_cv_enable_other
  else
    ac_cv_enable_other=$enable_other
  fi
  
  if test "$enable_other" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    UIPSUBDIRS="$UIPSUBDIRS other"
  fi


  echo $ac_n "checking whether compiling send""... $ac_c" 1>&6
  if test "$enable_send" != "yes" && test "$enable_send" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_send'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_send="yes"
fi

    enable_send=$ac_cv_enable_send
  else
    ac_cv_enable_send=$enable_send
  fi
  
  if test "$enable_send" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    UIPSUBDIRS="$UIPSUBDIRS send"
  fi


  echo $ac_n "checking whether compiling snd""... $ac_c" 1>&6
  if test "$enable_snd" != "yes" && test "$enable_snd" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_snd'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_snd="yes"
fi

    enable_snd=$ac_cv_enable_snd
  else
    ac_cv_enable_snd=$enable_snd
  fi
  
  if test "$enable_snd" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    UIPSUBDIRS="$UIPSUBDIRS snd"
  fi


  echo $ac_n "checking whether compiling ucbmail""... $ac_c" 1>&6
  if test "$enable_ucbmail" != "yes" && test "$enable_ucbmail" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_ucbmail'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_ucbmail="no"
fi

    enable_ucbmail=$ac_cv_enable_ucbmail
  else
    ac_cv_enable_ucbmail=$enable_ucbmail
  fi
  
  if test "$enable_ucbmail" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    UIPSUBDIRS="$UIPSUBDIRS ucbmail"
  fi


  echo $ac_n "checking whether compiling unsupported""... $ac_c" 1>&6
  if test "$enable_unsupported" != "yes" && test "$enable_unsupported" != "no"; then
    if eval "test \"`echo '$''{'ac_cv_enable_unsupported'+set}'`\" = set"; then
  echo $ac_n "(cached) $ac_c" 1>&6
else
  ac_cv_enable_unsupported="no"
fi

    enable_unsupported=$ac_cv_enable_unsupported
  else
    ac_cv_enable_unsupported=$enable_unsupported
  fi
  
  if test "$enable_unsupported" != yes; then
    echo "$ac_t""no" 1>&6
  else
    echo "$ac_t""yes" 1>&6
    UIPSUBDIRS="$UIPSUBDIRS unsupported"
  fi



CFLAGS="-I\${top_srcdir}/h $CFLAGS"

if test "$enable_fhs" != no; then
  
if test "x$prefix" = "xNONE"; then
   prefix=$ac_default_prefix
fi
if test "x$exec_prefix" = "xNONE"; then
   exec_prefix="\${prefix}"
fi
if test "$libexecdir" = "\${exec_prefix}/libexec"; then
   libexecdir="\${exec_prefix}/lib/mh"
fi
if test "$mtbldir" = "\${mmdfprefix}/table"; then
   mtbldir="\${sysconfdir}"
fi
if test "$datadir" = "\${prefix}/share"; then
   datadir="\${prefix}/share/mmdf"
fi
if test "$sysconfdir" = "\${prefix}/etc"; then
   sysconfdir="/etc/mmdf"
fi
mmtailor="\${sysconfdir}/mmdftailor"
tbldbm="\${mtbldir}/mmdfdbm"
lckdfldir="\${uucplock}/mmdf"
authfile="\${datadir}/warning"
uucplock="\${varprefix}/lock"
resendprog="\${bindir}/resend"
sendprog="\${bindir}/send"

   else
  
if test "x$prefix" = "xNONE"; then
   prefix=$ac_default_prefix
fi
if test "$mmdfprefix" = "\${prefix}/lib/mmdf"; then
   mmdfprefix="\${prefix}/mmdf"
fi
if test "x$exec_prefix" = "xNONE"; then
   exec_prefix="\${prefix}/local"
fi
if test "$bindir" = "\${exec_prefix}/bin"; then
   bindir="\${exec_prefix}/bin"
fi
if test "$sbindir" = "\${exec_prefix}/sbin"; then
   sbindir="\${exec_prefix}/sbin"
fi
if test "$libexecdir" = "\${exec_prefix}/libexec"; then
   libexecdir="\${exec_prefix}/lib/mh"
fi
if test "$datadir" = "\${prefix}/share"; then
   datadir="\${mmdfprefix}/table"
fi
if test "$varprefix" = "/var"; then
   varprefix="/var/spool"
fi
if test "$mquedir" = "\${varprefix}/spool/mmdf/home"; then
   mquedir="\${varprefix}/mmdf/home"
fi
if test "$mphsdir" = "\${varprefix}/state/mmdf"; then
   mphsdir="\${varprefix}/mmdf/log/phase"
fi
if test "$mlogdir" = "\${varprefix}/log/mmdf"; then
   mlogdir="\${varprefix}/mmdf/log"
fi
if test "$sharedstatedir" != "\${prefix}/com"; then
      echo "used old syntax --sharedstatedir="
   if test "$used_mquedir" = 0; then
      mquedir=$sharedstatedir
   else
      echo "configure: warning: "Ignoring option 'sharedstatedir='"" 1>&2
   fi
fi
if test "$localstatedir" != "\${prefix}/var"; then
      echo "used old syntax --localstatedir="
   if test "$used_mlogdir" = 0 && test "$used_mphsdir" = 0 && \
	test "$used_d_calllog" = 0 ; then
      mlogdir=$localstatedir
      mphsdir=$localstatedir/phase
      d_calllog=$localstatedir/dial_log
   else
      echo "configure: warning: "Ignoring option 'localstatedir'"" 1>&2
   fi
fi
if test "$sysconfdir" = "\${prefix}/etc"; then
   sysconfdir="\${mmdfprefix}"
fi
mmtailor="\${sysconfdir}/mmdftailor"
tbldbm="\${mtbldir}/mmdfdbm"
lckdfldir="/tmp/mmdf"
authfile="\${sysconfdir}/warning"
uucplock="/var/spool/locks"
resendprog="\${bindir}/resend"
sendprog="\${bindir}/send"

fi





























echo "host  ="$host
echo "cpu   ="$host_cpu
echo "vendor="$host_vendor
echo "os    ="$host_os
echo "target="$target

cp confdefs.h Xconfdefs.h
OUTPUT_FILES=$OUTPUT_FILES" "$OUTPUT_C_FILES
trap '' 1 2 15
cat > confcache <<\EOF
# This file is a shell script that caches the results of configure
# tests run on this system so they can be shared between configure
# scripts and configure runs.  It is not useful on other systems.
# If it contains results you don't want to keep, you may remove or edit it.
#
# By default, configure uses ./config.cache as the cache file,
# creating it if it does not exist already.  You can give configure
# the --cache-file=FILE option to use a different cache file; that is
# what configure does when it calls configure scripts in
# subdirectories, so they share the cache.
# Giving --cache-file=/dev/null disables caching, for debugging configure.
# config.status only pays attention to the cache file if you give it the
# --recheck option to rerun configure.
#
EOF
# Ultrix sh set writes to stderr and can't be redirected directly,
# and sets the high bit in the cache file unless we assign to the vars.
(set) 2>&1 |
  sed -n "s/^\([a-zA-Z0-9_]*_cv_[a-zA-Z0-9_]*\)=\(.*\)/\1=\${\1='\2'}/p" \
  >> confcache
if cmp -s $cache_file confcache; then
  :
else
  if test -w $cache_file; then
    echo "updating cache $cache_file"
    cat confcache > $cache_file
  else
    echo "not updating unwritable cache $cache_file"
  fi
fi
rm -f confcache

trap 'rm -fr conftest* confdefs* core core.* *.core $ac_clean_files; exit 1' 1 2 15

test "x$prefix" = xNONE && prefix=$ac_default_prefix
# Let make expand exec_prefix.
test "x$exec_prefix" = xNONE && exec_prefix='${prefix}'

# Any assignment to VPATH causes Sun make to only execute
# the first set of double-colon rules, so remove it if not needed.
# If there is a colon in the path, we need to keep it.
if test "x$srcdir" = x.; then
  ac_vpsub='/^[ 	]*VPATH[ 	]*=[^:]*$/d'
fi

trap 'rm -f $CONFIG_STATUS conftest*; exit 1' 1 2 15

DEFS=-DHAVE_CONFIG_H

# Without the "./", some shells look in PATH for config.status.
: ${CONFIG_STATUS=./config.status}

echo creating $CONFIG_STATUS
rm -f $CONFIG_STATUS
cat > $CONFIG_STATUS <<EOF
#! /bin/sh
# Generated automatically by configure.
# Run this file to recreate the current configuration.
# This directory was configured as follows,
# on host `(hostname || uname -n) 2>/dev/null | sed 1q`:
#
# $0 $ac_configure_args
#
# Compiler output produced by configure, useful for debugging
# configure, is in ./config.log if it exists.

ac_cs_usage="Usage: $CONFIG_STATUS [--recheck] [--version] [--help]"
for ac_option
do
  case "\$ac_option" in
  -recheck | --recheck | --rechec | --reche | --rech | --rec | --re | --r)
    echo "running \${CONFIG_SHELL-/bin/sh} $0 $ac_configure_args --no-create --no-recursion"
    exec \${CONFIG_SHELL-/bin/sh} $0 $ac_configure_args --no-create --no-recursion ;;
  -version | --version | --versio | --versi | --vers | --ver | --ve | --v)
    echo "$CONFIG_STATUS generated by autoconf version 2.10"
    exit 0 ;;
  -help | --help | --hel | --he | --h)
    echo "\$ac_cs_usage"; exit 0 ;;
  *) echo "\$ac_cs_usage"; exit 1 ;;
  esac
done

ac_given_srcdir=$srcdir
ac_given_INSTALL="$INSTALL"

trap 'rm -fr `echo "${OUTPUT_FILES} h/config.h" | sed "s/:[^ ]*//g"` conftest*; exit 1' 1 2 15
EOF
cat >> $CONFIG_STATUS <<EOF

# Protect against being on the right side of a sed subst in config.status.
sed 's/%@/@@/; s/@%/@@/; s/%g\$/@g/; /@g\$/s/[\\\\&%]/\\\\&/g;
 s/@@/%@/; s/@@/@%/; s/@g\$/%g/' > conftest.subs <<\\CEOF
$ac_vpsub
$extrasub
s%@CFLAGS@%$CFLAGS%g
s%@CPPFLAGS@%$CPPFLAGS%g
s%@CXXFLAGS@%$CXXFLAGS%g
s%@DEFS@%$DEFS%g
s%@LDFLAGS@%$LDFLAGS%g
s%@LIBS@%$LIBS%g
s%@exec_prefix@%$exec_prefix%g
s%@prefix@%$prefix%g
s%@program_transform_name@%$program_transform_name%g
s%@bindir@%$bindir%g
s%@sbindir@%$sbindir%g
s%@libexecdir@%$libexecdir%g
s%@datadir@%$datadir%g
s%@sysconfdir@%$sysconfdir%g
s%@sharedstatedir@%$sharedstatedir%g
s%@localstatedir@%$localstatedir%g
s%@libdir@%$libdir%g
s%@includedir@%$includedir%g
s%@oldincludedir@%$oldincludedir%g
s%@infodir@%$infodir%g
s%@mandir@%$mandir%g
s%@varprefix@%$varprefix%g
s%@phasedir@%$phasedir%g
s%@logdir@%$logdir%g
s%@spooldir@%$spooldir%g
s%@diallog@%$diallog%g
s%@mmdfprefix@%$mmdfprefix%g
s%@mcmddir@%$mcmddir%g
s%@mchndir@%$mchndir%g
s%@mtbldir@%$mtbldir%g
s%@mphsdir@%$mphsdir%g
s%@mlogdir@%$mlogdir%g
s%@d_calllog@%$d_calllog%g
s%@mquedir@%$mquedir%g
s%@MMDF_MAJOR_VERSION@%$MMDF_MAJOR_VERSION%g
s%@MMDF_MINOR_VERSION@%$MMDF_MINOR_VERSION%g
s%@MMDF_PATCH_LEVEL@%$MMDF_PATCH_LEVEL%g
s%@VERSION_DATE@%$VERSION_DATE%g
s%@VERSION@%$VERSION%g
s%@mmdflogin@%$mmdflogin%g
s%@mmdfgroup@%$mmdfgroup%g
s%@ROOTLOGIN@%$ROOTLOGIN%g
s%@host@%$host%g
s%@host_alias@%$host_alias%g
s%@host_cpu@%$host_cpu%g
s%@host_vendor@%$host_vendor%g
s%@host_os@%$host_os%g
s%@CC@%$CC%g
s%@CPP@%$CPP%g
s%@PATH_GCC@%$PATH_GCC%g
s%@PIPE@%$PIPE%g
s%@ELF@%$ELF%g
s%@MYDEFS@%$MYDEFS%g
s%@AWK@%$AWK%g
s%@INSTALL_PROGRAM@%$INSTALL_PROGRAM%g
s%@INSTALL_DATA@%$INSTALL_DATA%g
s%@LN_S@%$LN_S%g
s%@SET_MAKE@%$SET_MAKE%g
s%@AR@%$AR%g
s%@XSTR@%$XSTR%g
s%@CTAGS@%$CTAGS%g
s%@D_DIALPROG@%$D_DIALPROG%g
s%@ASSIGN@%$ASSIGN%g
s%@CHOWN@%$CHOWN%g
s%@LORDER@%$LORDER%g
s%@VPRINT@%$VPRINT%g
s%@RANLIB@%$RANLIB%g
s%@SUBDIRS@%$SUBDIRS%g
s%@LIBSUBDIRS@%$LIBSUBDIRS%g
s%@SRCSUBDIRS@%$SRCSUBDIRS%g
s%@UIPSUBDIRS@%$UIPSUBDIRS%g
s%@WHAT_DEPEND@%$WHAT_DEPEND%g
s%@DEPEND_FLAG@%$DEPEND_FLAG%g
s%@PGMPROT@%$PGMPROT%g
s%@MMDFLIBS@%$MMDFLIBS%g
s%@CXXTEMPLFLAGS@%$CXXTEMPLFLAGS%g
s%@STATIC_LDFLAGS@%$STATIC_LDFLAGS%g
s%@CH_TB@%$CH_TB%g
s%@LIBFIX@%$LIBFIX%g
s%@UNAME_MACHINE@%$UNAME_MACHINE%g
s%@UNAME_RELEASE@%$UNAME_RELEASE%g
s%@UNAME_SYSTEM@%$UNAME_SYSTEM%g
s%@UNAME_VERSION@%$UNAME_VERSION%g
s%@UNAME_NODE@%$UNAME_NODE%g
s%@rcvdir@%$rcvdir%g
s%@mmtailor@%$mmtailor%g
s%@tbldbm@%$tbldbm%g
s%@lckdfldir@%$lckdfldir%g
s%@authfile@%$authfile%g
s%@uucplock@%$uucplock%g
s%@resendprog@%$resendprog%g
s%@sendprog@%$sendprog%g

CEOF
EOF
cat >> $CONFIG_STATUS <<EOF

CONFIG_FILES=\${CONFIG_FILES-"${OUTPUT_FILES}"}
EOF
cat >> $CONFIG_STATUS <<\EOF
for ac_file in .. $CONFIG_FILES; do if test "x$ac_file" != x..; then
  # Support "outfile[:infile]", defaulting infile="outfile.in".
  case "$ac_file" in
  *:*) ac_file_in=`echo "$ac_file"|sed 's%.*:%%'`
       ac_file=`echo "$ac_file"|sed 's%:.*%%'` ;;
  *) ac_file_in="${ac_file}.in" ;;
  esac

  # Adjust relative srcdir, etc. for subdirectories.

  # Remove last slash and all that follows it.  Not all systems have dirname.
  ac_dir=`echo $ac_file|sed 's%/[^/][^/]*$%%'`
  if test "$ac_dir" != "$ac_file" && test "$ac_dir" != .; then
    # The file is in a subdirectory.
    test ! -d "$ac_dir" && mkdir "$ac_dir"
    ac_dir_suffix="/`echo $ac_dir|sed 's%^\./%%'`"
    # A "../" for each directory in $ac_dir_suffix.
    ac_dots=`echo $ac_dir_suffix|sed 's%/[^/]*%../%g'`
  else
    ac_dir_suffix= ac_dots=
  fi

  case "$ac_given_srcdir" in
  .)  srcdir=.
      if test -z "$ac_dots"; then top_srcdir=.
      else top_srcdir=`echo $ac_dots|sed 's%/$%%'`; fi ;;
  /*) srcdir="$ac_given_srcdir$ac_dir_suffix"; top_srcdir="$ac_given_srcdir" ;;
  *) # Relative path.
    srcdir="$ac_dots$ac_given_srcdir$ac_dir_suffix"
    top_srcdir="$ac_dots$ac_given_srcdir" ;;
  esac

  case "$ac_given_INSTALL" in
  [/$]*) INSTALL="$ac_given_INSTALL" ;;
  *) INSTALL="$ac_dots$ac_given_INSTALL" ;;
  esac
  echo creating "$ac_file"
  rm -f "$ac_file"
  configure_input="Generated automatically from `echo $ac_file_in|sed 's%.*/%%'` by configure."
  case "$ac_file" in
  *Makefile*) ac_comsub="1i\\
# $configure_input" ;;
  *) ac_comsub= ;;
  esac
  sed -e "$ac_comsub
s%@configure_input@%$configure_input%g
s%@srcdir@%$srcdir%g
s%@top_srcdir@%$top_srcdir%g
s%@INSTALL@%$INSTALL%g
" -f conftest.subs $ac_given_srcdir/$ac_file_in > $ac_file
fi; done
rm -f conftest.subs

# These sed commands are passed to sed as "A NAME B NAME C VALUE D", where
# NAME is the cpp macro being defined and VALUE is the value it is being given.
#
# ac_d sets the value in "#define NAME VALUE" lines.
ac_dA='s%^\([ 	]*\)#\([ 	]*define[ 	][ 	]*\)'
ac_dB='\([ 	][ 	]*\)[^ 	]*%\1#\2'
ac_dC='\3'
ac_dD='%g'
# ac_u turns "#undef NAME" with trailing blanks into "#define NAME VALUE".
ac_uA='s%^\([ 	]*\)#\([ 	]*\)undef\([ 	][ 	]*\)'
ac_uB='\([ 	]\)%\1#\2define\3'
ac_uC=' '
ac_uD='\4%g'
# ac_e turns "#undef NAME" without trailing blanks into "#define NAME VALUE".
ac_eA='s%^\([ 	]*\)#\([ 	]*\)undef\([ 	][ 	]*\)'
ac_eB='$%\1#\2define\3'
ac_eC=' '
ac_eD='%g'

CONFIG_HEADERS=${CONFIG_HEADERS-"h/config.h"}
for ac_file in .. $CONFIG_HEADERS; do if test "x$ac_file" != x..; then
  # Support "outfile[:infile]", defaulting infile="outfile.in".
  case "$ac_file" in
  *:*) ac_file_in=`echo "$ac_file"|sed 's%.*:%%'`
       ac_file=`echo "$ac_file"|sed 's%:.*%%'` ;;
  *) ac_file_in="${ac_file}.in" ;;
  esac

  echo creating $ac_file

  rm -f conftest.frag conftest.in conftest.out
  cp $ac_given_srcdir/$ac_file_in conftest.in

EOF

# Transform confdefs.h into a sed script conftest.vals that substitutes
# the proper values into config.h.in to produce config.h.  And first:
# Protect against being on the right side of a sed subst in config.status.
# Protect against being in an unquoted here document in config.status.
rm -f conftest.vals
cat > conftest.hdr <<\EOF
s/[\\&%]/\\&/g
s%[\\$`]%\\&%g
s%#define \([A-Za-z_][A-Za-z0-9_]*\) *\(.*\)%${ac_dA}\1${ac_dB}\1${ac_dC}\2${ac_dD}%gp
s%ac_d%ac_u%gp
s%ac_u%ac_e%gp
EOF
sed -n -f conftest.hdr confdefs.h > conftest.vals
rm -f conftest.hdr

# This sed command replaces #undef with comments.  This is necessary, for
# example, in the case of _POSIX_SOURCE, which is predefined and required
# on some systems where configure will not decide to define it.
cat >> conftest.vals <<\EOF
s%^[ 	]*#[ 	]*undef[ 	][ 	]*[a-zA-Z_][a-zA-Z_0-9]*%/* & */%
EOF

# Break up conftest.vals because some shells have a limit on
# the size of here documents, and old seds have small limits too.
# Maximum number of lines to put in a single here document.
ac_max_here_lines=12

rm -f conftest.tail
while :
do
  ac_lines=`grep -c . conftest.vals`
  # grep -c gives empty output for an empty file on some AIX systems.
  if test -z "$ac_lines" || test "$ac_lines" -eq 0; then break; fi
  # Write a limited-size here document to conftest.frag.
  echo '  cat > conftest.frag <<CEOF' >> $CONFIG_STATUS
  sed ${ac_max_here_lines}q conftest.vals >> $CONFIG_STATUS
  echo 'CEOF
  sed -f conftest.frag conftest.in > conftest.out
  rm -f conftest.in
  mv conftest.out conftest.in
' >> $CONFIG_STATUS
  sed 1,${ac_max_here_lines}d conftest.vals > conftest.tail
  rm -f conftest.vals
  mv conftest.tail conftest.vals
done
rm -f conftest.vals

cat >> $CONFIG_STATUS <<\EOF
  rm -f conftest.frag conftest.h
  echo "/* $ac_file.  Generated automatically by configure.  */" > conftest.h
  cat conftest.in >> conftest.h
  rm -f conftest.in
  if cmp -s $ac_file conftest.h 2>/dev/null; then
    echo "$ac_file is unchanged"
    rm -f conftest.h
  else
    # Remove last slash and all that follows it.  Not all systems have dirname.
      ac_dir=`echo $ac_file|sed 's%/[^/][^/]*$%%'`
      if test "$ac_dir" != "$ac_file" && test "$ac_dir" != .; then
      # The file is in a subdirectory.
      test ! -d "$ac_dir" && mkdir "$ac_dir"
    fi
    rm -f $ac_file
    mv conftest.h $ac_file
  fi
fi; done



exit 0
EOF
chmod +x $CONFIG_STATUS
rm -fr confdefs* $ac_clean_files
test "$no_create" = yes || ${CONFIG_SHELL-/bin/sh} $CONFIG_STATUS || exit 1


if test "$MMDF_TARGET" = "yes"; then
   cd conf
   echo running ./sitesetup $target
   ./sitesetup $target
   cd ..
else
   echo "No target found. Cannot run ./sitesetup <target>"
   echo "Please do it by hand."
fi

rm -f Makefile.com
ln -s conf/common/Makefile.com Makefile.com

echo "==============================================================="
echo "
MMDF version                   : $MMDF_MAJOR_VERSION.$MMDF_MINOR_VERSION
   Patchlevel                  : $MMDF_PATCH_LEVEL
   Date                        : $VERSION_DATE
compiler                       : ${CC}
compiler flags                 : ${CFLAGS} ${PIPE}
linker flags                   : ${LDFLAGS}
source code location           : ${srcdir}

   Here are your pathname-settings:
prefix                         = $prefix
exec_prefix                    = $exec_prefix
varprefix                      = $varprefix
bindir                         = $bindir
sbindir                        = $sbindir
libexecdir                     = $libexecdir
sysconfdir                     = $sysconfdir

mmdfprefix                     = $mmdfprefix
mcmddir                        = $mcmddir
mchndir                        = $mchndir
mtbldir                        = $mtbldir
datadir                        = $datadir
configdir                      = $sysconfdir
tbldbm                         = $tbldbm
authfile                       = $authfile

mquedir                        = $mquedir
mphsdir                        = $mphsdir
mlogdir                        = $mlogdir
uucplock                       = $uucplock
lckdfldir                      = $lckdfldir
d_calllog                      = $d_calllog

resendprog                     = $resendprog
sendprog                       = $sendprog"

